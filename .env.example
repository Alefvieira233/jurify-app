# =====================================================================
# JURIFY — Environment Variables
# =====================================================================
# Copy this file to .env and fill in the values.
# Variables prefixed with VITE_ are exposed to the browser.
# NEVER put secret keys with the VITE_ prefix.
# =====================================================================

# ==========================================
# 1. SUPABASE (obrigatório)
# ==========================================
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# ==========================================
# 2. SENTRY — Error Monitoring (obrigatório em produção)
# ==========================================
VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
VITE_APP_VERSION=1.0.0

# Build-time only (CI/CD) — NÃO prefixar com VITE_
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=jurify
SENTRY_AUTH_TOKEN=sntrys_...

# ==========================================
# 3. STRIPE — Pagamentos (frontend)
# ==========================================
VITE_STRIPE_PRICE_PRO=price_...
VITE_STRIPE_PRICE_ENTERPRISE=price_...

# ==========================================
# 5. GOOGLE CALENDAR — OAuth (frontend — apenas CLIENT_ID é público)
# ==========================================
VITE_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
VITE_GOOGLE_REDIRECT_URI=https://app.jurify.com.br/auth/google/callback

# ==========================================
# 6. LOGGING (opcional)
# ==========================================
VITE_ENABLE_CONSOLE_LOGS=false
VITE_LOG_LEVEL=info
# VITE_LOG_ENDPOINT=https://your-log-service.com/ingest

# =====================================================================
# SUPABASE SECRETS (Edge Functions — server-side only)
# Configure via: Supabase Dashboard → Settings → Edge Functions → Secrets
# Ou via CLI: npx supabase secrets set KEY=VALUE
# =====================================================================
#
# OPENAI_API_KEY=sk-...
# EVOLUTION_API_URL=https://evo.yourdomain.com
# EVOLUTION_API_KEY=your-evolution-api-key
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# ZAPSIGN_API_TOKEN=your-zapsign-token
# ENCRYPTION_KEY=your-256-bit-hex-key (gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# SUPABASE_SERVICE_ROLE_KEY=eyJ... (auto-set by Supabase)
# =====================================================================
