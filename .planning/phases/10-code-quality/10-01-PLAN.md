---
phase: 10-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/integrations/supabase/types.ts
  - src/hooks/useConexoes.ts
  - src/hooks/useDepartamentos.ts
  - src/hooks/useLeadHistorico.ts
  - src/hooks/useLeadNotas.ts
  - src/hooks/useLeadTagsBatch.ts
  - src/hooks/useRBAC.ts
  - src/hooks/useTags.ts
  - src/hooks/useTeamMembers.ts
  - src/components/OnboardingFlow.tsx
  - src/features/equipe/EquipeManager.tsx
  - src/features/conexoes/ConnectionDetailsDrawer.tsx
  - src/features/whatsapp/WhatsAppKapsoSetup.tsx
  - src/utils/logger.ts
  - src/utils/__tests__/logger.test.ts
  - src/utils/monitoring.ts
  - src/lib/monitoring.ts
  - package.json
  - src/contexts/AuthContext.tsx
autonomous: true
requirements: [QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-07, QUAL-09]

must_haves:
  truths:
    - "Zero local supabase as any casts — all use canonical supabaseUntyped import"
    - "src/utils/logger.ts and its test deleted"
    - "Only one monitoring.ts file exists"
    - "rrule removed from package.json"
    - "Zero stray console.log/error/warn in production code (outside logger infrastructure)"
    - "hasPermission returns boolean synchronously"
  artifacts:
    - path: "src/hooks/useRBAC.ts"
      provides: "Uses canonical supabaseUntyped import"
      contains: "import.*supabaseUntyped.*from.*client"
    - path: "src/contexts/AuthContext.tsx"
      provides: "Synchronous hasPermission"
      contains: "hasPermission.*boolean"
---

<objective>
Quick wins: replace 12 local as-any casts with canonical import, delete dead code, consolidate monitoring, remove unused deps, fix console calls, make hasPermission synchronous.

Purpose: Low-effort, high-impact code quality improvements.
Output: Cleaner codebase with consistent patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 12 local as-any casts with canonical supabaseUntyped import</name>
  <files>
    src/hooks/useConexoes.ts,
    src/hooks/useDepartamentos.ts,
    src/hooks/useLeadHistorico.ts,
    src/hooks/useLeadNotas.ts,
    src/hooks/useLeadTagsBatch.ts,
    src/hooks/useRBAC.ts,
    src/hooks/useTags.ts,
    src/hooks/useTeamMembers.ts,
    src/components/OnboardingFlow.tsx,
    src/features/equipe/EquipeManager.tsx,
    src/features/conexoes/ConnectionDetailsDrawer.tsx,
    src/features/whatsapp/WhatsAppKapsoSetup.tsx
  </files>
  <read_first>
    src/integrations/supabase/client.ts (canonical supabaseUntyped export),
    src/hooks/useConexoes.ts,
    src/hooks/useDepartamentos.ts,
    src/hooks/useRBAC.ts
  </read_first>
  <action>
**QUAL-02 — Replace local casts with canonical import.**

In each of the 12 files:

1. Remove the local `as any` cast line:
   - `const db = supabase as any` or
   - `const supabaseUntyped = supabase as any`

2. Add/update the import to use the canonical export:
   ```typescript
   import { supabaseUntyped } from '@/integrations/supabase/client';
   ```
   If the file already imports `supabase` from the client, change it to also import `supabaseUntyped`.

3. If the local variable was named `db`, replace all usages of `db` with `supabaseUntyped` in the file.

4. Remove the now-unused `supabase` import if only `supabaseUntyped` is needed.

**Files and their local cast patterns:**
- `useConexoes.ts:8` — `const supabaseUntyped = supabase as any` (already same name, just fix import)
- `useDepartamentos.ts:8` — `const db = supabase as any` (rename db → supabaseUntyped)
- `useLeadHistorico.ts:6` — `const db = supabase as any`
- `useLeadNotas.ts:8` — `const db = supabase as any`
- `useLeadTagsBatch.ts:7` — `const db = supabase as any`
- `useRBAC.ts:8` — `const supabaseUntyped = supabase as any`
- `useTags.ts:8` — `const db = supabase as any`
- `useTeamMembers.ts:7` — `const db = supabase as any`
- `OnboardingFlow.tsx:13` — `const db = supabase as any`
- `EquipeManager.tsx:19` — `const db = supabase as any`
- `ConnectionDetailsDrawer.tsx:23` — `const supabaseUntyped = supabase as any`
- `WhatsAppKapsoSetup.tsx:30` — `const supabaseUntyped = supabase as any`
  </action>
  <verify>
    <automated>cd E:/Jurify && grep -rn "= supabase as any\|supabase as any" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__tests__" | grep -v "client.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "= supabase as any" src/` returns zero matches outside of client.ts and test files
    - All 12 files import `supabaseUntyped` from `@/integrations/supabase/client`
    - No local `const db = supabase as any` or `const supabaseUntyped = supabase as any` lines remain
    - `npm run build` passes
  </acceptance_criteria>
  <done>All 12 local casts replaced with canonical import</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code, consolidate monitoring, remove deps, fix console calls, sync hasPermission</name>
  <files>
    src/utils/logger.ts,
    src/utils/__tests__/logger.test.ts,
    src/utils/monitoring.ts,
    src/lib/monitoring.ts,
    package.json,
    src/features/automations/FluxosManager.tsx,
    src/features/whatsapp/WhatsAppKapsoSetup.tsx,
    src/contexts/AuthContext.tsx
  </files>
  <read_first>
    src/utils/logger.ts,
    src/utils/monitoring.ts,
    src/lib/monitoring.ts,
    src/features/ai-agents/AgentesIAManager.tsx (imports from utils/monitoring),
    src/features/automations/FluxosManager.tsx,
    src/features/whatsapp/WhatsAppKapsoSetup.tsx,
    src/contexts/AuthContext.tsx,
    package.json
  </read_first>
  <action>
**QUAL-03 — Delete dead logger:**
Delete `src/utils/logger.ts` and `src/utils/__tests__/logger.test.ts`. No production code imports them.

**QUAL-04 — Consolidate monitoring:**
1. Read `src/utils/monitoring.ts` to find business-specific methods (`trackLeadConversion`, `trackContractSigned`, etc.)
2. Copy those methods into `src/lib/monitoring.ts`
3. Update the single consumer (`src/features/ai-agents/AgentesIAManager.tsx`) to import from `@/lib/monitoring` instead of `@/utils/monitoring`
4. Delete `src/utils/monitoring.ts`

**QUAL-05 — Remove unused rrule:**
In `package.json`, remove:
- `"rrule": "^2.8.1"` from `dependencies`
- `"@types/rrule": "^2.1.7"` from `dependencies`

**QUAL-07 — Replace 5 stray console calls:**
In `FluxosManager.tsx`:
- Replace `console.error('Erro ao salvar fluxo:', err)` with `createLogger('FluxosManager').error('Erro ao salvar fluxo:', err)`. Import `createLogger` from `@/lib/logger`.

In `WhatsAppKapsoSetup.tsx`:
- The file already imports `createLogger`. Replace 4 `console.log/error/warn` calls with the logger instance:
  - `console.log(...)` → `logger.debug(...)`
  - `console.error(...)` → `logger.error(...)`
  - `console.warn(...)` → `logger.warn(...)`

**QUAL-09 — Make hasPermission synchronous:**
In `src/contexts/AuthContext.tsx`:
1. Find the `hasPermission` function (returns `Promise<boolean>`)
2. Change return type from `Promise<boolean>` to `boolean`
3. Remove `async` keyword
4. The function body likely already does a synchronous lookup in `ROLE_PERMISSIONS` — just remove the async wrapper
5. Update the AuthContext type definition to match: `hasPermission: (resource: string, action: string) => boolean`
  </action>
  <verify>
    <automated>cd E:/Jurify && test ! -f src/utils/logger.ts && test ! -f src/utils/monitoring.ts && grep -c "rrule" package.json && grep -rn "console\.\(log\|error\|warn\)" src/features/automations/FluxosManager.tsx src/features/whatsapp/WhatsAppKapsoSetup.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/logger.ts` does not exist
    - `src/utils/__tests__/logger.test.ts` does not exist
    - `src/utils/monitoring.ts` does not exist
    - `src/lib/monitoring.ts` exists and contains business tracking methods
    - `grep "rrule" package.json` returns zero matches
    - `grep "console\." src/features/automations/FluxosManager.tsx` returns zero matches (outside logger infra)
    - `grep "console\." src/features/whatsapp/WhatsAppKapsoSetup.tsx` returns zero matches
    - `AuthContext.tsx` has `hasPermission` returning `boolean` (not `Promise<boolean>`)
    - `npm run build` passes
    - `npm run test` passes
  </acceptance_criteria>
  <done>Dead code deleted, monitoring consolidated, deps cleaned, console calls fixed, hasPermission sync</done>
</task>

</tasks>

<verification>
1. Zero local `as any` casts in production code
2. Dead files deleted
3. `npm run build` passes
4. `npm run test` passes
</verification>

<success_criteria>
- QUAL-02: All local casts use canonical import
- QUAL-03: Dead logger deleted
- QUAL-04: Monitoring consolidated
- QUAL-05: rrule removed
- QUAL-07: No stray console calls
- QUAL-09: hasPermission is synchronous
- All tests pass, 0 TS errors
</success_criteria>
