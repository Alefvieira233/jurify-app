---
phase: 06-kapso-frontend-conexoes-redesign
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/features/conexoes/__tests__/ConexoesManager.test.tsx
  - src/features/conexoes/__tests__/ConnectionDetailsDrawer.test.tsx
  - src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx
autonomous: true
requirements: [FR-17, FR-18, NFR-1]

must_haves:
  truths:
    - "ConexoesManager renders connection table with status badges"
    - "ConexoesManager shows empty state with Kapso branding"
    - "ConnectionDetailsDrawer renders tabs (geral, logs, config, acoes, alertas, diagnostico)"
    - "WhatsAppKapsoSetup test updated to mock conexoes_whatsapp instead of configuracoes_integracoes"
    - "All new tests pass alongside existing 1202+ tests"
  artifacts:
    - path: "src/features/conexoes/__tests__/ConexoesManager.test.tsx"
      provides: "ConexoesManager component tests"
      min_lines: 60
    - path: "src/features/conexoes/__tests__/ConnectionDetailsDrawer.test.tsx"
      provides: "ConnectionDetailsDrawer component tests"
      min_lines: 40
    - path: "src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx"
      provides: "Updated WhatsAppKapsoSetup tests for conexoes_whatsapp table"
      contains: "conexoes_whatsapp"
  key_links:
    - from: "src/features/conexoes/__tests__/ConexoesManager.test.tsx"
      to: "src/features/conexoes/ConexoesManager.tsx"
      via: "import ConexoesManager"
      pattern: "ConexoesManager"
---

<objective>
Add test coverage for the Conexoes feature components and update the existing WhatsAppKapsoSetup test to match the Plan 01 changes (conexoes_whatsapp instead of configuracoes_integracoes).

Purpose: Ensure the Kapso frontend migration has test coverage (FR-17, FR-18) and update existing tests to reflect the new data source. ConexoesManager and ConnectionDetailsDrawer currently have zero tests.
Output: Test files for ConexoesManager and ConnectionDetailsDrawer; updated WhatsAppKapsoSetup test.
</objective>

<execution_context>
@.planning/phases/06-kapso-frontend-conexoes-redesign/06-02-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-kapso-frontend-conexoes-redesign/06-01-SUMMARY.md
@src/features/conexoes/ConexoesManager.tsx
@src/features/conexoes/ConnectionDetailsDrawer.tsx
@src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx
@src/hooks/useConexoes.ts

<interfaces>
From src/hooks/useConexoes.ts:
```typescript
export interface ConexaoWhatsApp {
  id: string; tenant_id: string; nome: string; telefone: string | null;
  tipo: 'kapso' | 'oficial' | 'cloud_api'; provider: string;
  instance_name: string | null; status: string;
  status_padrao: string | null; departamento_id: string | null;
  responsavel_id: string | null; avatar_url: string | null;
  last_heartbeat: string | null; last_sync: string | null;
  last_error: string | null; reconnect_attempts: number;
  config: Record<string, unknown>; created_at: string; updated_at: string;
  departamento?: { id: string; nome: string; cor: string } | null;
  responsavel?: { id: string; nome_completo: string; email: string } | null;
}
```

From src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx (mock pattern):
```typescript
vi.mock('@/integrations/supabase/client', () => ({
  supabase: {
    auth: { getSession: () => mockGetSession() },
    functions: { invoke: mockInvoke },
    from: () => ({
      select: () => ({ eq: () => ({ maybeSingle: () => mockMaybeSingle() }) }),
    }),
  },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConexoesManager and ConnectionDetailsDrawer tests</name>
  <files>src/features/conexoes/__tests__/ConexoesManager.test.tsx, src/features/conexoes/__tests__/ConnectionDetailsDrawer.test.tsx</files>
  <action>
    read_first:
    - src/features/conexoes/ConexoesManager.tsx
    - src/features/conexoes/ConnectionDetailsDrawer.tsx
    - src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx (for mock patterns)
    - src/hooks/useConexoes.ts

    **ConexoesManager.test.tsx:**
    Create test file following the same mock patterns as WhatsAppKapsoSetup.test.tsx. Use vi.hoisted for mock functions.

    Mock useConexoes via `vi.mock('@/hooks/useConexoes')` returning configurable data:
    - mockConexoes: array of ConexaoWhatsApp objects
    - isLoading, deleteConexao, etc.

    Mock useRBAC to return `{ can: () => true, canManageIntegrations: true }`.
    Mock usePageTitle as no-op.
    Mock useToast.

    Test cases:
    1. "renders header with title 'Conexoes'" - check for heading text
    2. "renders 'Nova Conexao' button when user has create permission" - check button presence
    3. "renders connection table with connection data" - provide mockConexoes with 1 item, check nome and telefone appear
    4. "renders status badge in table for connected connection" - provide connection with status 'connected', check 'Conectado' text
    5. "renders empty state when no connections exist" - provide empty array, check empty state text
    6. "filters connections by search term" - provide 2 connections, type in search, verify filtering
    7. "shows Kapso QR branding in empty state" - check the empty state cards show "Kapso QR" text

    **ConnectionDetailsDrawer.test.tsx:**
    Mock useConexaoLogs and useConexaoAlertas from useConexoes.
    Mock useConexoes (for deleteConexao).
    Mock useRBAC, useToast, supabase.

    Test cases:
    1. "renders nothing when conexao is null" - pass null, check nothing renders
    2. "renders connection name and status in header" - pass conexao with nome and status
    3. "renders 6 tab triggers" - check for Geral, Logs, Configuracoes, Acoes, Alertas, Diagnostico
    4. "renders general info on Geral tab" - check for tipo, provider, instance_name display
    5. "shows unresolved alert count badge" - provide alertas with unresolved items

    acceptance_criteria:
    - ls src/features/conexoes/__tests__/ConexoesManager.test.tsx exists
    - ls src/features/conexoes/__tests__/ConnectionDetailsDrawer.test.tsx exists
    - npx vitest run src/features/conexoes/__tests__/ passes all tests
  </action>
  <verify>
    <automated>cd /e/Jurify && npx vitest run src/features/conexoes/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ConexoesManager.test.tsx has 7 passing test cases
    - ConnectionDetailsDrawer.test.tsx has 5 passing test cases
    - All tests pass with zero failures
  </done>
</task>

<task type="auto">
  <name>Task 2: Update WhatsAppKapsoSetup test to use conexoes_whatsapp mock</name>
  <files>src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx</files>
  <action>
    read_first:
    - src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx
    - src/features/whatsapp/WhatsAppKapsoSetup.tsx (to see the updated query from Plan 01)

    Update the existing test to match Plan 01 changes:

    1. The supabase mock's `from()` chain currently returns data matching the configuracoes_integracoes schema (id, status, observacoes). Update mockMaybeSingle responses to match conexoes_whatsapp schema instead:
       - Change `{ id: '1', status: 'ativa', observacoes: 'Instance: jurify_abc' }` to `{ id: '1', status: 'connected', nome: 'WhatsApp Principal', instance_name: 'jurify_abc' }`
    2. Update the mock chain to match the new query structure. The component now calls:
       `from('conexoes_whatsapp').select('id, status, nome, instance_name').eq('tenant_id', tenantId).eq('tipo', 'kapso').maybeSingle()`
       So the mock chain needs an additional `.eq()` in the chain: `from: () => ({ select: () => ({ eq: () => ({ eq: () => ({ maybeSingle: () => mockMaybeSingle() }) }) }) })`
    3. Update the test "loads existing instance from DB on mount" — it should now expect 'Conectado' badge when status is 'connected'.
    4. Update the test "shows disconnect and remove buttons when connected" — same data shape update.
    5. Ensure all 10 existing tests still pass with the updated mocks.

    acceptance_criteria:
    - grep "conexoes_whatsapp" src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx is found as a comment indicating the table being mocked
    - grep "instance_name" src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx returns matches (new schema field)
    - grep -c "observacoes" src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx returns 0 (old schema field removed)
    - npx vitest run src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx passes all tests
  </action>
  <verify>
    <automated>cd /e/Jurify && npx vitest run src/features/whatsapp/__tests__/WhatsAppKapsoSetup.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - WhatsAppKapsoSetup.test.tsx uses conexoes_whatsapp schema in mocks
    - No references to observacoes or configuracoes_integracoes schema
    - All 10 existing test cases pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to validate NFR-1 compliance</name>
  <files></files>
  <action>
    read_first: (none - verification only)

    1. Run `npm test -- --run` and confirm all tests pass (existing 1202+ plus new ~12 tests = ~1214+).
    2. Run `npx tsc --noEmit` to confirm zero TypeScript errors.
    3. Run `npm run lint` to confirm zero warnings.

    acceptance_criteria:
    - npm test -- --run reports 1210+ tests passing
    - npx tsc --noEmit exits with code 0
    - npm run lint exits with code 0
  </action>
  <verify>
    <automated>cd /e/Jurify && npm test -- --run 2>&1 | tail -10; npx tsc --noEmit 2>&1 | tail -3; npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Total test count increased by ~12 (ConexoesManager 7 + ConnectionDetailsDrawer 5)
    - All tests pass (NFR-1)
    - Zero TypeScript errors, zero lint warnings (NFR-2)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/features/conexoes/__tests__/ --reporter=verbose` all pass
- `npx vitest run src/features/whatsapp/__tests__/ --reporter=verbose` all pass
- `npm test -- --run` reports 1210+ tests passing
- `npm run lint` exits cleanly
</verification>

<success_criteria>
ConexoesManager and ConnectionDetailsDrawer have test coverage. WhatsAppKapsoSetup tests updated to match Kapso-only data source. All NFR requirements maintained with increased test count.
</success_criteria>

<output>
After completion, create `.planning/phases/06-kapso-frontend-conexoes-redesign/06-02-SUMMARY.md`
</output>
