---
phase: 06-kapso-frontend-conexoes-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useConexoes.ts
  - src/features/whatsapp/WhatsAppKapsoSetup.tsx
  - src/features/conexoes/ConexoesManager.tsx
  - src/features/conexoes/ConnectionDetailsDrawer.tsx
autonomous: true
requirements: [FR-17, FR-18, FR-20, NFR-1, NFR-2]

must_haves:
  truths:
    - "useConexoes hook only queries conexoes_whatsapp table (no configuracoes_integracoes fallback)"
    - "WhatsAppKapsoSetup loads existing instance from conexoes_whatsapp, not configuracoes_integracoes"
    - "ConexoesManager uses typed supabase client where possible"
    - "All existing 1202+ tests still pass"
    - "Zero TypeScript errors and zero lint warnings"
  artifacts:
    - path: "src/hooks/useConexoes.ts"
      provides: "Kapso-only connection hook without legacy fallback"
      contains: "conexoes_whatsapp"
    - path: "src/features/whatsapp/WhatsAppKapsoSetup.tsx"
      provides: "WhatsApp Kapso setup with conexoes_whatsapp table"
      contains: "conexoes_whatsapp"
    - path: "src/features/conexoes/ConexoesManager.tsx"
      provides: "Conexoes table page"
      contains: "ConexoesManager"
  key_links:
    - from: "src/features/conexoes/ConexoesManager.tsx"
      to: "src/hooks/useConexoes.ts"
      via: "useConexoes import"
      pattern: "useConexoes"
    - from: "src/features/whatsapp/WhatsAppKapsoSetup.tsx"
      to: "supabase.functions.invoke"
      via: "kapso-manager Edge Function"
      pattern: "kapso-manager"
---

<objective>
Remove all legacy configuracoes_integracoes fallbacks from connection hooks and components, making the frontend fully Kapso-native against the conexoes_whatsapp table. Polish ConexoesManager with consistent status display.

Purpose: Complete the frontend migration to Kapso-only, matching the backend migration done in Phase 5. The useConexoes hook still has a fallback to the old configuracoes_integracoes table and WhatsAppKapsoSetup still reads from it on mount.
Output: Clean Kapso-only frontend with no legacy table references.
</objective>

<execution_context>
@.planning/phases/06-kapso-frontend-conexoes-redesign/06-01-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kapso-backend-migration/05-03-SUMMARY.md
@src/hooks/useConexoes.ts
@src/features/whatsapp/WhatsAppKapsoSetup.tsx
@src/features/conexoes/ConexoesManager.tsx
@src/features/conexoes/ConnectionDetailsDrawer.tsx

<interfaces>
From src/hooks/useConexoes.ts:
```typescript
export interface ConexaoWhatsApp {
  id: string;
  tenant_id: string;
  nome: string;
  telefone: string | null;
  tipo: 'kapso' | 'oficial' | 'cloud_api';
  provider: string;
  instance_name: string | null;
  status: string;
  // ... (full interface in file)
}

export function useConexoes(): {
  conexoes: ConexaoWhatsApp[];
  isLoading: boolean;
  error: Error | null;
  createConexao: (input: Partial<ConexaoWhatsApp>) => Promise<unknown>;
  updateConexao: (input: Partial<ConexaoWhatsApp> & { id: string }) => Promise<unknown>;
  deleteConexao: (id: string) => Promise<void>;
  isCreating: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy configuracoes_integracoes fallback from useConexoes and WhatsAppKapsoSetup</name>
  <files>src/hooks/useConexoes.ts, src/features/whatsapp/WhatsAppKapsoSetup.tsx</files>
  <action>
    read_first:
    - src/hooks/useConexoes.ts
    - src/features/whatsapp/WhatsAppKapsoSetup.tsx

    **useConexoes.ts changes:**
    1. Remove the entire fallback block in conexoesQuery.queryFn that catches the error from conexoes_whatsapp and falls back to querying configuracoes_integracoes (lines 76-108). Instead, if the query to conexoes_whatsapp fails, just throw the error.
    2. Remove the console.warn about "not available, using fallback".
    3. Keep the `supabaseUntyped` cast since conexoes_whatsapp is not in generated Supabase types yet.
    4. Keep all other functionality (createMutation, updateMutation, deleteMutation, useConexaoLogs, useConexaoAlertas) unchanged.

    **WhatsAppKapsoSetup.tsx changes:**
    1. In the useEffect that loads existing instance on mount (around line 131-170), change the query from `configuracoes_integracoes` with `nome_integracao = 'whatsapp_kapso'` to query `conexoes_whatsapp` instead.
    2. Use `supabaseUntyped` (add the same cast pattern as useConexoes.ts) to query: `from('conexoes_whatsapp').select('id, status, nome, instance_name').eq('tenant_id', tenantId).eq('tipo', 'kapso').maybeSingle()`.
    3. Update the state mapping: status 'connected' maps to state 'connected', 'disconnected' maps to 'disconnected', anything else maps to 'idle'.
    4. Set instanceName from `data.instance_name` instead of extracting from `observacoes`.
    5. Remove the old `extractInstanceName` helper function.

    acceptance_criteria:
    - grep -r "configuracoes_integracoes" src/hooks/useConexoes.ts returns 0 matches
    - grep -r "configuracoes_integracoes" src/features/whatsapp/WhatsAppKapsoSetup.tsx returns 0 matches
    - grep "conexoes_whatsapp" src/hooks/useConexoes.ts returns matches
    - grep "conexoes_whatsapp" src/features/whatsapp/WhatsAppKapsoSetup.tsx returns matches
  </action>
  <verify>
    <automated>cd /e/Jurify && grep -c "configuracoes_integracoes" src/hooks/useConexoes.ts src/features/whatsapp/WhatsAppKapsoSetup.tsx; npx tsc --noEmit 2>&1 | tail -5; npx eslint src/hooks/useConexoes.ts src/features/whatsapp/WhatsAppKapsoSetup.tsx --max-warnings 0</automated>
  </verify>
  <done>
    - useConexoes.ts has zero references to configuracoes_integracoes
    - WhatsAppKapsoSetup.tsx has zero references to configuracoes_integracoes
    - Both files query conexoes_whatsapp directly
    - TypeScript compiles with zero errors
    - ESLint passes with zero warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Add status badge consistency and polish to ConexoesManager table</name>
  <files>src/features/conexoes/ConexoesManager.tsx</files>
  <action>
    read_first:
    - src/features/conexoes/ConexoesManager.tsx
    - src/features/conexoes/ConnectionDetailsDrawer.tsx (for STATUS_BADGE pattern reference)

    1. Add a status column to the ConexoesManager table showing a colored Badge for each connection status (connected/disconnected/error), reusing the same STATUS_BADGE mapping pattern from ConnectionDetailsDrawer.tsx. Add it after the CONEXAO column.
    2. Add a "tipo" indicator showing "Kapso QR" or "API Oficial" or "Cloud API" as a small secondary text under the connection name, replacing the current bare phone number display. Show phone number inline with the tipo indicator: e.g., "Kapso QR | +55 11 99999-0000".
    3. In the empty state (no connections), update the two card titles: change "API Nao Oficial" to "Kapso QR" and "API Oficial" to "Kapso Oficial" to match the Kapso branding established in Phase 5.
    4. In ConnectionTypeChooser.tsx, update the same labels: "API Nao Oficial" -> "Kapso QR", "API Oficial (Cloud API)" -> "Kapso Oficial (Cloud API)".

    acceptance_criteria:
    - grep "Kapso QR" src/features/conexoes/ConexoesManager.tsx returns at least 1 match
    - grep "Kapso Oficial" src/features/conexoes/ConexoesManager.tsx returns at least 1 match
    - grep "Kapso QR" src/features/conexoes/ConnectionTypeChooser.tsx returns at least 1 match
    - grep "STATUS" src/features/conexoes/ConexoesManager.tsx returns matches (status column header)
    - grep -c "API Nao Oficial" src/features/conexoes/ConexoesManager.tsx returns 0
    - grep -c "API Nao Oficial" src/features/conexoes/ConnectionTypeChooser.tsx returns 0
  </action>
  <verify>
    <automated>cd /e/Jurify && npx tsc --noEmit 2>&1 | tail -5; npx eslint src/features/conexoes/ConexoesManager.tsx src/features/conexoes/ConnectionTypeChooser.tsx --max-warnings 0; grep -c "API Não Oficial" src/features/conexoes/ConexoesManager.tsx src/features/conexoes/ConnectionTypeChooser.tsx || true</automated>
  </verify>
  <done>
    - ConexoesManager table has a visible status badge column
    - Connection tipo shown as "Kapso QR" / "Kapso Oficial" in table and empty state
    - ConnectionTypeChooser labels updated to Kapso branding
    - Zero TypeScript errors and zero lint warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and build to validate NFR compliance</name>
  <files></files>
  <action>
    read_first: (none - verification only)

    1. Run `npm test -- --run` and confirm all 1202+ tests pass.
    2. Run `npm run lint` and confirm zero warnings.
    3. Run `npm run build` and confirm production build succeeds.
    4. Run `grep -ri "configuracoes_integracoes" src/` to confirm zero legacy table references remain in frontend source.
    5. Run `grep -ri "evolution" src/` to confirm the Evolution-free state from Phase 5 is maintained.

    acceptance_criteria:
    - npm test passes with 1200+ tests
    - npm run lint exits with code 0
    - npm run build exits with code 0
    - grep -ri "configuracoes_integracoes" src/hooks/useConexoes.ts returns 0 matches
    - grep -ri "configuracoes_integracoes" src/features/whatsapp/WhatsAppKapsoSetup.tsx returns 0 matches
    - grep -ri "evolution" src/ returns 0 matches
  </action>
  <verify>
    <automated>cd /e/Jurify && npm test -- --run 2>&1 | tail -10; npm run lint 2>&1 | tail -5; npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 1202+ tests pass (NFR-1)
    - Zero lint warnings (NFR-2)
    - Production build succeeds
    - No configuracoes_integracoes references in useConexoes or WhatsAppKapsoSetup
    - No evolution references in src/
  </done>
</task>

</tasks>

<verification>
- `grep -ri "configuracoes_integracoes" src/hooks/useConexoes.ts src/features/whatsapp/WhatsAppKapsoSetup.tsx` returns 0 matches
- `grep -ri "evolution" src/` returns 0 matches
- `npm test -- --run` passes with 1200+ tests
- `npm run lint` exits cleanly (0 warnings)
- `npm run build` succeeds
</verification>

<success_criteria>
Frontend is fully Kapso-native: useConexoes hook and WhatsAppKapsoSetup query conexoes_whatsapp directly with no legacy fallback. ConexoesManager displays status badges and Kapso branding consistently. All NFR requirements maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/06-kapso-frontend-conexoes-redesign/06-01-SUMMARY.md`
</output>
