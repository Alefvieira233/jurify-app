---
phase: 04-liderhub-new-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/suporte/SuportePage.tsx
  - src/features/suporte/TicketDetailDialog.tsx
  - src/hooks/useTicketsSuporte.ts
  - src/features/suporte/__tests__/SuportePage.test.tsx
autonomous: true
requirements: [FR-11, NFR-1, NFR-2]

must_haves:
  truths:
    - "User can create a support ticket with tipo and conteudo"
    - "User can view ticket details in a dialog"
    - "User can update ticket status (aberto -> em_andamento -> fechado)"
    - "User can filter tickets by tipo"
    - "User can rate a closed ticket (1-5 stars)"
  artifacts:
    - path: "src/features/suporte/TicketDetailDialog.tsx"
      provides: "Ticket detail view with status update and rating"
      min_lines: 60
    - path: "src/features/suporte/__tests__/SuportePage.test.tsx"
      provides: "Unit tests for SuportePage"
      min_lines: 30
  key_links:
    - from: "src/features/suporte/SuportePage.tsx"
      to: "src/hooks/useTicketsSuporte.ts"
      via: "useTicketsSuporte hook"
      pattern: "updateTicket\\.mutate"
    - from: "src/features/suporte/TicketDetailDialog.tsx"
      to: "src/hooks/useTicketsSuporte.ts"
      via: "status update and rating mutations"
      pattern: "updateTicket|rateTicket"
---

<objective>
Enhance the Suporte module with ticket detail view, status management, type filter, and ticket rating. The SuportePage and useTicketsSuporte hook already exist with create/read. This plan adds update, detail view, and filtering.

Purpose: Complete the Suporte feature (FR-11) with full ticket lifecycle management.
Output: Enhanced SuportePage with detail dialog, status update, type filter, rating, and tests.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/hooks/useTicketsSuporte.ts:
```typescript
export interface TicketSuporte {
  id: string;
  tenant_id: string;
  criador_id: string;
  tipo: 'duvida' | 'bug' | 'sugestao' | 'outro';
  conteudo: string;
  status: 'aberto' | 'em_andamento' | 'fechado';
  avaliacao: number | null;
  created_at: string;
  updated_at: string;
}

export function useTicketsSuporte(): {
  tickets: TicketSuporte[];
  isLoading: boolean;
  createTicket: UseMutationResult;
}
```

DB schema (tickets_suporte):
- status CHECK (status IN ('aberto', 'em_andamento', 'fechado'))
- avaliacao INT CHECK (avaliacao BETWEEN 1 AND 5)
- RLS: tenant isolation on all operations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateTicket mutation to hook, create TicketDetailDialog, enhance SuportePage</name>
  <files>
    src/hooks/useTicketsSuporte.ts,
    src/features/suporte/TicketDetailDialog.tsx,
    src/features/suporte/SuportePage.tsx
  </files>
  <action>
    read_first:
    - src/hooks/useTicketsSuporte.ts
    - src/features/suporte/SuportePage.tsx
    - src/hooks/useTarefas.ts (for mutation pattern reference)

    1. Enhance `src/hooks/useTicketsSuporte.ts`:
       - Add `updateTicket` mutation: accepts `{ id: string; status?: string; avaliacao?: number }`, calls supabase.from('tickets_suporte').update({...values, updated_at: new Date().toISOString()}).eq('id', id). On success: invalidate queryKey ['tickets-suporte']. On error: toast with destructive variant.
       - Return `updateTicket` from hook alongside existing returns.

    2. Create `src/features/suporte/TicketDetailDialog.tsx`:
       - Props: `{ ticket: TicketSuporte | null; open: boolean; onOpenChange: (open: boolean) => void }`
       - Uses Dialog/DialogContent/DialogHeader/DialogTitle from shadcn
       - Shows: ticket.conteudo (full text), ticket.tipo (badge), ticket.status (badge), ticket.created_at (formatted)
       - Status update: 3 buttons (Aberto, Em Andamento, Fechado) — active state highlighted. On click: call `updateTicket.mutate({ id: ticket.id, status: newStatus })`
       - Rating section (only visible when status === 'fechado'):
         - 5 star buttons (Star icon from lucide-react). Filled stars up to current rating.
         - On star click: call `updateTicket.mutate({ id: ticket.id, avaliacao: starNumber })`
       - Import useTicketsSuporte from hook

    3. Enhance `src/features/suporte/SuportePage.tsx`:
       - Add `tipoFilter` state (string, default '')
       - Add tipo `<select>` filter next to search: Todos, Duvida, Bug, Sugestao, Outro
       - Update filtered logic to also check `tipoFilter`
       - Add `selectedTicket` state (`TicketSuporte | null`)
       - Add `detailOpen` state (boolean)
       - Make each table row clickable: onClick sets `selectedTicket` and `detailOpen`
       - Add cursor-pointer class to rows
       - Add avaliacao column to table showing star count or dash
       - Render `<TicketDetailDialog ticket={selectedTicket} open={detailOpen} onOpenChange={setDetailOpen} />`
       - Destructure `updateTicket` from `useTicketsSuporte()` (already done by TicketDetailDialog internally)

    acceptance_criteria:
    - grep "updateTicket" src/hooks/useTicketsSuporte.ts returns matches
    - grep "TicketDetailDialog" src/features/suporte/SuportePage.tsx returns matches
    - grep "tipoFilter" src/features/suporte/SuportePage.tsx returns matches
    - grep "avaliacao" src/features/suporte/TicketDetailDialog.tsx returns matches
    - File exists: src/features/suporte/TicketDetailDialog.tsx
  </action>
  <verify>
    <automated>cd /e/Jurify && npx tsc --noEmit 2>&1 | head -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>SuportePage has ticket detail dialog with status update and rating, type filter, and clickable rows. useTicketsSuporte exports updateTicket. Zero TS errors, zero lint warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Add SuportePage unit tests</name>
  <files>src/features/suporte/__tests__/SuportePage.test.tsx</files>
  <action>
    read_first:
    - src/features/suporte/SuportePage.tsx
    - src/features/home/__tests__/HomePage.test.tsx (for project test patterns)

    Create `src/features/suporte/__tests__/SuportePage.test.tsx`:
    - Mock `@/hooks/useTicketsSuporte` returning controlled data
    - Mock `@/hooks/usePageTitle` as no-op

    Test cases:
    1. "renders ticket list" — provide 2 mock tickets, verify conteudo text appears in table
    2. "renders empty state when no tickets" — check for "Nenhum ticket encontrado"
    3. "filters by search text" — type in search, verify filtering
    4. "shows tipo filter select" — verify the tipo filter select element exists
    5. "opens detail dialog on row click" — click row, verify dialog title appears

    Use vi.mock(), render() from @testing-library/react, screen, userEvent.

    acceptance_criteria:
    - File exists at src/features/suporte/__tests__/SuportePage.test.tsx
    - grep "describe" returns match
    - At least 3 test cases
  </action>
  <verify>
    <automated>cd /e/Jurify && npx vitest run src/features/suporte/__tests__/SuportePage.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All SuportePage tests pass. Tests cover rendering, filtering, and detail dialog interaction.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run lint` passes with zero warnings
- `npx vitest run src/features/suporte/` passes all tests
- SuportePage renders with detail dialog, status update, type filter, rating
</verification>

<success_criteria>
- TicketDetailDialog.tsx exists with status update buttons and star rating
- useTicketsSuporte exports updateTicket mutation
- SuportePage has type filter and clickable rows opening detail dialog
- All tests pass, zero TS errors, zero lint warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-liderhub-new-features/04-02-SUMMARY.md`
</output>
