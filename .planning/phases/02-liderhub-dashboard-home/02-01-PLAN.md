---
phase: 02-liderhub-dashboard-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/home/HomePage.tsx
  - src/features/dashboard/components/StatCard.tsx
  - src/features/home/__tests__/HomePage.test.tsx
autonomous: true
requirements: [FR-6, NFR-1, NFR-2]

must_haves:
  truths:
    - "HomePage displays a time-of-day greeting with the user's first name"
    - "HomePage shows 4 stat cards with real data from hooks (leads hoje, total leads, tarefas pendentes, agendamentos)"
    - "HomePage shows quick action buttons that navigate to correct routes"
    - "StatCard component supports optional sparkline, icon, change indicator"
  artifacts:
    - path: "src/features/home/HomePage.tsx"
      provides: "Redesigned home page with real data from useLeads, useTarefas, useAgendamentos"
      min_lines: 60
    - path: "src/features/dashboard/components/StatCard.tsx"
      provides: "Enhanced StatCard with sparkline support"
      exports: ["default"]
    - path: "src/features/home/__tests__/HomePage.test.tsx"
      provides: "Unit tests for HomePage"
      min_lines: 30
  key_links:
    - from: "src/features/home/HomePage.tsx"
      to: "src/hooks/useLeads.ts"
      via: "useLeads hook import"
      pattern: "import.*useLeads"
    - from: "src/features/home/HomePage.tsx"
      to: "src/hooks/useTarefas.ts"
      via: "useTarefas hook import"
      pattern: "import.*useTarefas"
    - from: "src/features/home/HomePage.tsx"
      to: "src/features/dashboard/components/StatCard.tsx"
      via: "StatCard component import"
      pattern: "import.*StatCard"
---

<objective>
Redesign the HomePage with real data from existing hooks and enhance the StatCard component to support sparklines.

Purpose: FR-6 requires a HomePage with greeting, stats summary, and quick actions. The current HomePage has placeholder dashes for tarefas/agendamentos and uses inline card markup instead of the reusable StatCard component.

Output: Enhanced HomePage with live data, enhanced StatCard with sparkline support, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-l-derhub-visual-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Hooks the executor will consume -->

From src/hooks/useLeads.ts:
```typescript
export const useLeads = (options?: { enablePagination?: boolean; pageSize?: number }) => {
  // returns:
  // leads: Lead[] (with .status: string, .created_at: string)
  // loading: boolean
  // error: string | null
};
```

From src/hooks/useTarefas.ts:
```typescript
export interface Tarefa {
  id: string; titulo: string; status: 'pendente' | 'em_andamento' | 'concluida' | 'cancelada';
  prioridade: 'baixa' | 'media' | 'alta' | 'urgente'; prazo: string | null; created_at: string;
}
export function useTarefas(): {
  tarefas: Tarefa[] | undefined; isLoading: boolean;
  // + mutations
};
```

From src/hooks/useAgendamentos.ts:
```typescript
export const useAgendamentos = () => {
  // returns: agendamentos: Agendamento[] (with .data_hora: string, .status: string)
  // loading: boolean
};
```

From src/features/dashboard/components/StatCard.tsx (current):
```typescript
interface StatCardProps {
  label: string;
  value: string | number;
  change?: string;
  changeType?: 'positive' | 'negative' | 'neutral';
  icon?: ReactNode;
}
export default function StatCard({ label, value, change, changeType, icon }: StatCardProps);
```

From src/contexts/AuthContext.tsx:
```typescript
useAuth() => { profile: { nome_completo: string; tenant_id: string; ... }, user, signOut }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance StatCard with sparkline and color props</name>
  <files>src/features/dashboard/components/StatCard.tsx</files>
  <read_first>src/features/dashboard/components/StatCard.tsx, src/features/dashboard/Dashboard.tsx (lines 64-82 for MiniSparkline reference)</read_first>
  <action>
Extend the StatCard component to support an optional inline SVG sparkline and accent color:

1. Add new optional props to StatCardProps:
   - `sparkData?: number[]` — array of 7 data points for mini sparkline
   - `sparkColor?: string` — stroke/fill color for the sparkline (default: "hsl(var(--primary))")
   - `className?: string` — allow consumers to pass additional classes

2. Move the MiniSparkline SVG logic currently inline in Dashboard.tsx (lines 64-82) into StatCard as an internal helper. The sparkline renders a polyline + filled polygon area, 80px wide, 28px tall, with strokeWidth 1.5 and fillOpacity 0.1.

3. Render the sparkline below the change text when `sparkData` is provided and has >= 2 points.

4. Keep full backward compatibility — existing StatCard usage in Dashboard.tsx must still work without sparkData/sparkColor props (they are optional).

5. Do NOT modify Dashboard.tsx in this task — it will continue using its inline MiniSparkline for now (Dashboard refactoring is not in Phase 2 scope).
  </action>
  <verify>
    <automated>cd /e/Jurify && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - StatCard.tsx exports a default function component
    - StatCardProps interface includes sparkData, sparkColor, className as optional fields
    - SVG sparkline renders when sparkData has >= 2 points
    - Existing StatCardProps (label, value, change, changeType, icon) still work unchanged
  </acceptance_criteria>
  <done>StatCard component accepts sparkData/sparkColor props and renders an inline SVG sparkline. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign HomePage with real data and StatCard</name>
  <files>src/features/home/HomePage.tsx, src/features/home/__tests__/HomePage.test.tsx</files>
  <read_first>src/features/home/HomePage.tsx, src/features/dashboard/components/StatCard.tsx, src/hooks/useTarefas.ts (lines 1-40), src/hooks/useAgendamentos.ts (lines 61-90), src/hooks/useLeads.ts (lines 164-175, 483-503)</read_first>
  <action>
Rewrite HomePage.tsx to use real data from existing hooks and the enhanced StatCard component:

**1. Imports:**
- `useAuth` from `@/contexts/AuthContext`
- `usePageTitle` from `@/hooks/usePageTitle`
- `useLeads` from `@/hooks/useLeads`
- `useTarefas` from `@/hooks/useTarefas`
- `useAgendamentos` from `@/hooks/useAgendamentos`
- `useNavigate` from `react-router-dom`
- `StatCard` from `@/features/dashboard/components/StatCard`
- Icons from `lucide-react`: `Plus, MessageCircle, CheckSquare, Calendar, Users, ListTodo, CalendarCheck, TrendingUp`
- `Button` from `@/components/ui/button`
- `Skeleton` from `@/components/ui/skeleton`

**2. Greeting section:**
- Time-of-day greeting: "Bom dia" (<12h), "Boa tarde" (<18h), "Boa noite" (else)
- Show user's first name from `profile?.nome_completo?.split(' ')[0]` fallback "Usuario"
- Subtitle: "Aqui esta o resumo do seu dia."
- Use `text-xl font-semibold` for greeting (Display size from UI-SPEC: 20px/600)

**3. Stats grid (4 StatCards in a `grid grid-cols-2 md:grid-cols-4 gap-4`):**
- Card 1: label="Leads hoje", value=count of leads where created_at starts with today's ISO date, icon=`<Users className="h-4 w-4" />`, changeType="neutral"
- Card 2: label="Total de leads", value=leads?.length ?? 0, icon=`<TrendingUp className="h-4 w-4" />`
- Card 3: label="Tarefas pendentes", value=count of tarefas where status === 'pendente', icon=`<ListTodo className="h-4 w-4" />`
- Card 4: label="Agendamentos", value=count of agendamentos where data_hora is today or future, icon=`<CalendarCheck className="h-4 w-4" />`
- Show `<Skeleton className="h-24" />` for each card slot while any hook is loading

**4. Quick actions section:**
- h2 "Acoes rapidas" with `text-sm font-semibold text-foreground mb-3`
- 4 buttons in `flex flex-wrap gap-3`:
  - "Novo Lead" (primary, navigates to /pipeline)
  - "Nova Conversa" (outline, navigates to /whatsapp)
  - "Nova Tarefa" (outline, navigates to /tarefas)
  - "Agendar" (outline, navigates to /agendamentos)
- Each button has an icon (Plus, MessageCircle, CheckSquare, Calendar) with `h-3.5 w-3.5 mr-1.5`

**5. PrazosUrgentesWidget:**
- Import `PrazosUrgentesWidget` from `@/features/dashboard/components/PrazosUrgentesWidget`
- Render it below the quick actions section
- Wrapping container: `max-w-5xl mx-auto`

**6. Overall layout:** `p-6 max-w-5xl mx-auto space-y-8`

**7. Create test file `src/features/home/__tests__/HomePage.test.tsx`:**
- Mock `useAuth` to return `{ profile: { nome_completo: 'Test User', tenant_id: 't1' }, user: { id: 'u1' } }`
- Mock `useLeads` to return `{ leads: [{ id: '1', status: 'novo', created_at: new Date().toISOString() }], loading: false }`
- Mock `useTarefas` to return `{ tarefas: [{ id: '1', status: 'pendente', titulo: 'Test', created_at: new Date().toISOString() }], isLoading: false }`
- Mock `useAgendamentos` to return `{ agendamentos: [{ id: '1', data_hora: new Date().toISOString(), status: 'agendado' }], loading: false }`
- Mock `useRBAC` to return `{ getLeadVisibilityScope: () => 'all', getUserDepartamentos: () => [] }`
- Mock `usePageTitle` as vi.fn()
- Mock `usePrazosProcessuais` to return `{ prazosUrgentes: [], loading: false }`
- Wrap in MemoryRouter

Tests:
- Renders greeting text (match /Bom dia|Boa tarde|Boa noite/)
- Renders "Test" as first name (getByText matching "Test")
- Renders all 4 stat card labels: "Leads hoje", "Total de leads", "Tarefas pendentes", "Agendamentos"
- Renders quick action buttons: "Novo Lead", "Nova Conversa", "Nova Tarefa", "Agendar"
- Renders stat values (expect "1" to appear for leads hoje, total leads, tarefas pendentes, agendamentos)
  </action>
  <verify>
    <automated>cd /e/Jurify && npx vitest run src/features/home/__tests__/HomePage.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - HomePage imports useLeads, useTarefas, useAgendamentos hooks
    - HomePage imports StatCard from @/features/dashboard/components/StatCard
    - HomePage renders 4 StatCard components in a grid
    - HomePage renders PrazosUrgentesWidget
    - Tests pass: greeting, stat labels, quick action buttons
    - No TypeScript errors (npx tsc --noEmit passes)
  </acceptance_criteria>
  <done>HomePage shows real data from 3 hooks via 4 StatCards, quick actions navigate correctly, PrazosUrgentesWidget is displayed, and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx vitest run src/features/home/__tests__/HomePage.test.tsx` — all tests pass
3. `npx vitest run src/features/dashboard/__tests__/Dashboard.test.tsx` — existing tests still pass (StatCard backward compat)
4. `npm run build` — build succeeds
</verification>

<success_criteria>
- HomePage displays greeting with user's first name
- 4 StatCards show real counts from useLeads, useTarefas, useAgendamentos
- Quick action buttons navigate to /pipeline, /whatsapp, /tarefas, /agendamentos
- PrazosUrgentesWidget renders on HomePage
- StatCard component supports sparkData/sparkColor props
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-liderhub-dashboard-home/02-01-SUMMARY.md`
</output>
