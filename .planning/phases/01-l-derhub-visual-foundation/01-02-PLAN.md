---
phase: 01-l-derhub-visual-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TopBar.tsx
autonomous: false
requirements:
  - FR-3
  - NFR-1
  - NFR-2
must_haves:
  truths:
    - "Clicking the workspace name opens a dropdown with the current workspace checked"
    - "Clicking the avatar opens a dropdown with Minha Conta and Sair items"
    - "Sair triggers signOut and Minha Conta navigates to /configuracoes?tab=perfil"
    - "Notifications bell has aria-label Notificacoes"
    - "Avatar button has aria-label Menu do usuario"
    - "All existing 1009+ tests pass"
  artifacts:
    - path: "src/components/TopBar.tsx"
      provides: "TopBar with workspace selector dropdown and avatar dropdown"
      contains: "DropdownMenu"
  key_links:
    - from: "src/components/TopBar.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() provides signOut and profile"
      pattern: "signOut"
    - from: "src/components/TopBar.tsx"
      to: "/configuracoes?tab=perfil"
      via: "navigate() on Minha Conta click"
      pattern: "configuracoes.*tab=perfil"
---

<objective>
Enhance TopBar with workspace selector dropdown and avatar dropdown menu, plus accessibility attributes.

Purpose: Complete the TopBar to match LiderHub design with interactive dropdowns for workspace switching and user account actions.
Output: Updated TopBar.tsx with DropdownMenu integrations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-l-derhub-visual-foundation/01-RESEARCH.md
@.planning/phases/01-l-derhub-visual-foundation/01-UI-SPEC.md

<interfaces>
<!-- shadcn DropdownMenu already installed at src/components/ui/dropdown-menu.tsx -->
<!-- Exports: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel -->

From src/contexts/AuthContext.tsx (useAuth hook):
```typescript
// Available from useAuth():
profile: { nome_completo: string; role: string; tenant_id: string; ... } | null
signOut: () => Promise<void>
user: User | null
```

From src/utils/formatting:
```typescript
export function getInitials(name: string | null): string;
export function getAvatarHex(name: string): string;
```

From src/components/TopBar.tsx (current interface):
```typescript
interface TopBarProps {
  onMenuToggle: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace selector and avatar dropdown to TopBar</name>
  <files>src/components/TopBar.tsx</files>
  <read_first>
    - src/components/TopBar.tsx (current full file — 74 lines)
    - src/components/ui/dropdown-menu.tsx (available exports)
    - src/contexts/AuthContext.tsx (useAuth interface, signOut method)
    - .planning/phases/01-l-derhub-visual-foundation/01-UI-SPEC.md (interaction contracts for workspace selector and avatar dropdown)
  </read_first>
  <action>
    Rewrite src/components/TopBar.tsx with these additions while preserving ALL existing functionality:

    1. ADD imports at top:
       ```typescript
       import { Check, ChevronDown } from 'lucide-react';
       import {
         DropdownMenu,
         DropdownMenuContent,
         DropdownMenuItem,
         DropdownMenuLabel,
         DropdownMenuSeparator,
         DropdownMenuTrigger,
       } from '@/components/ui/dropdown-menu';
       ```

    2. WORKSPACE SELECTOR (replace the static "Jurify" text div):
       Wrap the workspace area in a `<DropdownMenu>`:
       - Trigger: `<DropdownMenuTrigger asChild>` wrapping a `<Button variant="ghost">` containing:
         - The workspace name text (use `profile?.tenant_id ? 'Jurify' : 'Jurify'` — hardcoded for now, future multi-workspace)
         - `<ChevronDown className="h-3 w-3 ml-1 text-muted-foreground" />`
       - Add `aria-label="Selecionar workspace"` on the trigger Button
       - Content: `<DropdownMenuContent align="start" className="w-52">`
         - Item 1: "Jurify" with `<Check className="h-4 w-4 mr-2" />` prefix, `font-semibold` class
         - Separator
         - Item 2: disabled item with text "Mais workspaces" and a `<span className="text-xs text-muted-foreground ml-auto">Em breve</span>`

    3. NOTIFICATIONS BELL — add `aria-label="Notificacoes"` to the existing Bell Button (the accent-free ASCII version, not the accented one):
       Change: `<Button variant="ghost" size="icon" className="relative"`
       To: `<Button variant="ghost" size="icon" className="relative h-11 w-11" aria-label="Notificacoes"`
       (added h-11 w-11 for 44px touch target per spacing spec)

    4. AVATAR DROPDOWN (replace the static Avatar):
       Wrap the Avatar in a `<DropdownMenu>`:
       - Trigger: `<DropdownMenuTrigger asChild>` wrapping a `<Button variant="ghost" size="icon" className="h-11 w-11 rounded-full" aria-label="Menu do usuario">`
         - Inside: the existing `<Avatar className="h-8 w-8">` with AvatarFallback
       - Content: `<DropdownMenuContent align="end" className="w-48">`
         - Label: user name (`profile?.nome_completo || 'Usuario'`) in `<DropdownMenuLabel>` + role in `<p className="text-xs text-muted-foreground">`
         - Separator
         - Item: "Minha Conta" with onClick `() => navigate('/configuracoes?tab=perfil')`
         - Separator
         - Item: "Sair" with `className="text-destructive focus:text-destructive"` and onClick `() => { void signOut(); }`

    5. ADD signOut to the useAuth destructuring:
       Change: `const { profile } = useAuth();`
       To: `const { profile, signOut } = useAuth();`

    6. PRESERVE all existing elements: mobile hamburger, search trigger (with kbd shortcut), ThemeToggle, Bell with unreadCount badge.

    7. Ensure the header keeps `h-14` height and all its existing classes.
  </action>
  <verify>
    <automated>cd E:/Jurify && grep "DropdownMenu" src/components/TopBar.tsx | head -5 && grep 'aria-label="Notificacoes"' src/components/TopBar.tsx | head -1 && grep 'aria-label="Menu do usuario"' src/components/TopBar.tsx | head -1 && grep 'aria-label="Selecionar workspace"' src/components/TopBar.tsx | head -1 && grep "Minha Conta" src/components/TopBar.tsx | head -1 && grep "text-destructive" src/components/TopBar.tsx | head -1 && grep "configuracoes.*tab=perfil" src/components/TopBar.tsx | head -1 && grep "signOut" src/components/TopBar.tsx | head -3 && npx tsc --noEmit 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DropdownMenu" src/components/TopBar.tsx` returns 2+ matches (workspace + avatar menus)
    - `grep 'aria-label="Notificacoes"' src/components/TopBar.tsx` returns 1 match
    - `grep 'aria-label="Menu do usuario"' src/components/TopBar.tsx` returns 1 match
    - `grep 'aria-label="Selecionar workspace"' src/components/TopBar.tsx` returns 1 match
    - `grep "Minha Conta" src/components/TopBar.tsx` returns 1 match
    - `grep "Sair" src/components/TopBar.tsx` returns 1 match (in avatar dropdown)
    - `grep "text-destructive" src/components/TopBar.tsx` returns 1+ match (Sair item styling)
    - `grep "configuracoes.*tab=perfil" src/components/TopBar.tsx` returns 1 match
    - `grep "signOut" src/components/TopBar.tsx` returns 2+ matches (import + usage)
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` all tests pass
  </acceptance_criteria>
  <done>TopBar has workspace selector dropdown (with current workspace checked, future workspace disabled), avatar dropdown (Minha Conta + Sair), notifications bell with aria-label, all 44px touch targets, all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 1 changes</name>
  <files>src/components/TopBar.tsx</files>
  <action>
    No code changes — this is a human visual verification checkpoint. The executor should start the dev server with `npm run dev` and present the verification steps to the user.
  </action>
  <read_first>
    - src/components/TopBar.tsx (verify changes from Task 1 are present)
  </read_first>
  <what-built>
    Complete Phase 1 visual foundation: updated accent tokens (neutral gray hover states), Inter font, polished sidebar (no duplicate ThemeToggle, fixed search bar), TopBar with workspace selector dropdown and avatar dropdown, breadcrumb font-semibold on active crumb.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:8081
    2. SIDEBAR: Verify hover states on nav items show neutral gray (not blue tint)
    3. SIDEBAR: Verify search bar "Buscar..." is readable on white background (gray bg, not black)
    4. SIDEBAR: Verify ThemeToggle is NOT in the sidebar logo area
    5. TOPBAR: Click "Jurify" text — verify workspace dropdown appears with "Jurify" checked and "Mais workspaces (Em breve)" disabled
    6. TOPBAR: Click avatar — verify dropdown with "Minha Conta" and "Sair" (red text) appears
    7. TOPBAR: Click "Minha Conta" — verify navigates to /configuracoes?tab=perfil
    8. TOPBAR: Verify ThemeToggle is present in TopBar
    9. FONT: Verify body text renders in Inter (inspect element, check computed font-family)
    10. BREADCRUMBS: Navigate to /dashboard, verify breadcrumb shows "Dashboard" in semibold
    11. Toggle dark mode via ThemeToggle — verify dark theme still works correctly
    12. Run tests: `npm run test -- --run` — verify all 1009+ pass
  </how-to-verify>
  <verify>
    <automated>cd E:/Jurify && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 12 visual verification steps pass
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>User has visually approved the Phase 1 visual foundation changes.</done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx tsc --noEmit` zero TypeScript errors
3. `npm run test -- --run` all 1009+ tests pass
4. `npm run lint` zero warnings
5. TopBar has DropdownMenu for workspace and avatar
6. All accessibility aria-labels present
</verification>

<success_criteria>
- Workspace selector dropdown opens on click with current workspace checked
- Avatar dropdown opens with "Minha Conta" (navigates to settings) and "Sair" (destructive red, triggers signOut)
- Notifications bell has aria-label="Notificacoes"
- Avatar button has aria-label="Menu do usuario"
- All 44px touch targets on TopBar icon buttons
- All existing tests pass, zero TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-l-derhub-visual-foundation/01-02-SUMMARY.md`
</output>
