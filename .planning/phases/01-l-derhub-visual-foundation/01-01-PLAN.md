---
phase: 01-l-derhub-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - index.html
  - src/components/Sidebar.tsx
  - src/components/Breadcrumbs.tsx
autonomous: true
requirements:
  - FR-1
  - FR-2
  - FR-4
  - NFR-1
  - NFR-2
must_haves:
  truths:
    - "Sidebar hover states use neutral gray background, not blue tint"
    - "Body font renders as Inter across all pages"
    - "Sidebar search bar is visible and readable on white background"
    - "ThemeToggle does not appear in the sidebar logo area"
    - "Active breadcrumb uses font-semibold (weight 600)"
    - "All existing 1009+ tests pass after changes"
  artifacts:
    - path: "src/index.css"
      provides: "Updated accent tokens (neutral gray) in :root"
      contains: "--accent: 220 14% 96%"
    - path: "index.html"
      provides: "Inter font loaded from Google Fonts"
      contains: "family=Inter"
    - path: "src/components/Sidebar.tsx"
      provides: "Cleaned sidebar: no ThemeToggle, fixed search bar, semantic bg token"
    - path: "src/components/Breadcrumbs.tsx"
      provides: "Active crumb with font-semibold"
      contains: "font-semibold"
  key_links:
    - from: "src/index.css"
      to: "tailwind.config.ts"
      via: "CSS custom properties consumed by hsl(var(--accent))"
      pattern: "--accent: 220 14% 96%"
    - from: "index.html"
      to: "tailwind.config.ts"
      via: "Google Fonts link loads Inter which matches fontFamily.sans config"
      pattern: "family=Inter"
---

<objective>
Update design tokens to match LiderHub neutral accent palette, align body font to Inter, polish sidebar component (remove ThemeToggle duplication, fix search bar styling, use semantic sidebar tokens), and finalize breadcrumb typography.

Purpose: Establish the visual foundation (color tokens + font + sidebar) that all subsequent phases build upon.
Output: Updated index.css, index.html, Sidebar.tsx, Breadcrumbs.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-l-derhub-visual-foundation/01-RESEARCH.md
@.planning/phases/01-l-derhub-visual-foundation/01-UI-SPEC.md

<interfaces>
<!-- Tailwind config maps CSS vars to utility classes. No changes needed to tailwind.config.ts. -->
From tailwind.config.ts:
```typescript
accent: {
  DEFAULT: 'hsl(var(--accent))',
  foreground: 'hsl(var(--accent-foreground))'
},
sidebar: {
  DEFAULT: 'hsl(var(--sidebar-background))',
  accent: 'hsl(var(--sidebar-accent))',
  'accent-foreground': 'hsl(var(--sidebar-accent-foreground))',
}
```

From src/components/Sidebar.tsx (imports to preserve):
```typescript
import ThemeToggle from '@/components/ThemeToggle';  // REMOVE this import
import KeyboardShortcutsHelp from '@/components/KeyboardShortcutsHelp';  // KEEP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and font</name>
  <files>src/index.css, index.html</files>
  <read_first>
    - src/index.css (current token values, full file)
    - index.html (current Manrope font links)
    - tailwind.config.ts (verify fontFamily.sans is already Inter)
    - .planning/phases/01-l-derhub-visual-foundation/01-UI-SPEC.md (exact token values)
  </read_first>
  <action>
    In src/index.css `:root` block, make these exact changes (leave `.dark` block UNTOUCHED):

    1. Change `--accent` from `217 91% 97%` to `220 14% 96%`
    2. Change `--accent-foreground` from `217 91% 45%` to `222 47% 11%`
    3. Change `--sidebar-accent` from `217 91% 97%` to `220 14% 96%`
    4. Change `--sidebar-accent-foreground` from `217 91% 45%` to `222 47% 11%`
    5. Update the CSS comment on line 1 from "Manrope" to "Inter" (or remove if referencing Manrope)

    In index.html:
    1. Replace the Manrope Google Fonts preload URL:
       FROM: `https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700;800&display=swap`
       TO: `https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap`
    2. Apply this to ALL THREE link tags (preload, stylesheet with media=print, noscript fallback)
    3. Keep the preconnect links to fonts.googleapis.com and fonts.gstatic.com unchanged

    Do NOT modify tailwind.config.ts (fontFamily.sans is already `['Inter', 'system-ui', '-apple-system', 'sans-serif']`).
    Do NOT modify the `.dark` theme block in index.css.
  </action>
  <verify>
    <automated>cd E:/Jurify && grep -n "accent: 220 14% 96%" src/index.css | head -5 && grep -n "accent-foreground: 222 47% 11%" src/index.css | head -5 && grep -c "family=Inter" index.html && grep -c "Manrope" index.html && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "--accent: 220 14% 96%" src/index.css` returns exactly 2 matches (accent + sidebar-accent)
    - `grep "--accent-foreground: 222 47% 11%" src/index.css` returns exactly 2 matches
    - `grep "family=Inter" index.html` returns 3 matches (preload, stylesheet, noscript)
    - `grep "Manrope" index.html` returns 0 matches
    - `grep "217 91% 97%" src/index.css` returns 0 matches in :root (may still exist in .dark)
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Light-mode accent tokens are neutral gray (220 14% 96%), Inter font loaded via Google Fonts, dark mode unchanged, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Sidebar and Breadcrumbs</name>
  <files>src/components/Sidebar.tsx, src/components/Breadcrumbs.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (current full file — 394 lines)
    - src/components/Breadcrumbs.tsx (current full file — 57 lines)
    - src/components/ThemeToggle.tsx (to confirm import path being removed)
    - .planning/phases/01-l-derhub-visual-foundation/01-UI-SPEC.md (interaction contracts)
  </read_first>
  <action>
    In src/components/Sidebar.tsx, make these changes:

    1. REMOVE the ThemeToggle import: delete the line `import ThemeToggle from '@/components/ThemeToggle';`
    2. REMOVE the ThemeToggle JSX from the logo area: delete the entire block:
       ```
       <div className="opacity-50 hover:opacity-100 transition-opacity">
         <ThemeToggle />
       </div>
       ```
    3. Fix the sidebar search bar background: In the search `<button>` element (around line 331), change:
       FROM: `bg-black/20 hover:bg-black/40 border border-sidebar-border/20 text-sidebar-foreground/50 hover:text-sidebar-foreground transition-all duration-300 shadow-inner`
       TO: `bg-muted hover:bg-muted/80 border border-border text-muted-foreground hover:text-foreground transition-colors duration-150`
    4. Fix the search bar placeholder text: change `"Pesquisar..."` to `"Buscar..."` to match UI spec copywriting contract
    5. Fix the search bar kbd styling: change `bg-white/5 text-sidebar-foreground/40 border border-white/10` to `bg-muted text-muted-foreground border border-border`
    6. Change the root `<nav>` element background from `bg-background` to `bg-sidebar`: change class `bg-background text-sidebar-foreground` to `bg-sidebar text-sidebar-foreground`
    7. Add `aria-label="Buscar"` to the sidebar search button element

    In src/components/Breadcrumbs.tsx:
    1. Change the active (last) crumb class from `font-medium` to `font-semibold` — this is the class on the inner `<span>` at the condition `i === crumbs.length - 1`

    Do NOT change MAIN_NAV structure, RBAC logic, expandable section logic, or user footer.
    Do NOT change active state styling (text-primary + border-primary + bg-primary/5 — already correct per spec).
  </action>
  <verify>
    <automated>cd E:/Jurify && grep -c "ThemeToggle" src/components/Sidebar.tsx && grep "bg-muted hover:bg-muted/80" src/components/Sidebar.tsx | head -1 && grep "bg-sidebar" src/components/Sidebar.tsx | head -1 && grep 'Buscar\.\.\.' src/components/Sidebar.tsx | head -1 && grep 'aria-label="Buscar"' src/components/Sidebar.tsx | head -1 && grep "font-semibold" src/components/Breadcrumbs.tsx | head -1 && npx tsc --noEmit 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ThemeToggle" src/components/Sidebar.tsx` returns `0`
    - `grep "bg-black/20" src/components/Sidebar.tsx` returns 0 matches
    - `grep "bg-muted hover:bg-muted/80" src/components/Sidebar.tsx` returns 1 match
    - `grep "bg-sidebar " src/components/Sidebar.tsx` returns 1 match (the root nav element)
    - `grep 'Buscar\.\.\.' src/components/Sidebar.tsx` returns 1 match
    - `grep 'aria-label="Buscar"' src/components/Sidebar.tsx` returns 1 match
    - `grep "font-semibold" src/components/Breadcrumbs.tsx` returns 1 match
    - `grep "font-medium" src/components/Breadcrumbs.tsx` returns 0 matches
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` all tests pass (1009+ passing)
  </acceptance_criteria>
  <done>Sidebar has no ThemeToggle, search bar uses bg-muted with Buscar... placeholder and aria-label, root nav uses bg-sidebar token, breadcrumb active crumb uses font-semibold, all tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npx tsc --noEmit` zero TypeScript errors
3. `npm run test -- --run` all 1009+ tests pass
4. `npm run lint` zero warnings
5. `grep "217 91% 97%" src/index.css` returns matches ONLY in `.dark` block (not in `:root`)
6. `grep "Manrope" index.html` returns 0
7. `grep "ThemeToggle" src/components/Sidebar.tsx` returns 0
</verification>

<success_criteria>
- Light-mode accent tokens updated to neutral gray (220 14% 96%) — hover states are gray not blue
- Inter font loaded via Google Fonts, Manrope removed from index.html
- Sidebar search bar readable on white background (bg-muted)
- ThemeToggle only in TopBar, not duplicated in Sidebar
- Sidebar uses semantic bg-sidebar token
- Active breadcrumb uses font-semibold (600 weight)
- All existing tests pass, zero TS errors, zero lint warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-l-derhub-visual-foundation/01-01-SUMMARY.md`
</output>
