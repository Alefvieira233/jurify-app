---
phase: 03-liderhub-atendimento
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/pipeline/KanbanCard.tsx
  - src/features/pipeline/KanbanColumn.tsx
autonomous: true
requirements: [NFR-1, NFR-2]

must_haves:
  truths:
    - "KanbanCard uses design token CSS variables (--card, --border, --foreground) not raw Tailwind color values"
    - "KanbanColumn header left-border color comes from column.color prop (unchanged)"
    - "KanbanCard hover state uses var(--accent) shadow, not hard-coded shadow"
    - "KanbanColumn drop zone max-height uses calc with --topbar-height if defined, otherwise keeps 280px fallback"
    - "Zero visual regressions — all existing props/interfaces unchanged"
  artifacts:
    - path: "src/features/pipeline/KanbanCard.tsx"
      provides: "Polish: replace bg-card / border-border with token classes already in Phase 1 design system"
    - path: "src/features/pipeline/KanbanColumn.tsx"
      provides: "Polish: replace bg-white dark:bg-card with bg-card token"
  key_links:
    - from: "src/features/pipeline/KanbanCard.tsx"
      to: "src/index.css"
      via: "Tailwind bg-card, border-border, hover:shadow-sm classes using Phase 1 tokens"
      pattern: "bg-card|border-border"
---

<objective>
Apply LíderHub visual polish to KanbanCard and KanbanColumn — replace hard-coded color/dark-mode pairs with the Phase 1 design token classes so the Kanban board matches the rest of the redesigned UI.

Purpose: Phase 1 established CSS custom properties (--card, --border, --accent, etc.). KanbanColumn uses `bg-white dark:bg-card` (a pre-token pattern). KanbanCard uses `bg-card` already but its hover shadow is generic. This task aligns both with the token system.
Output: Polished KanbanCard.tsx + KanbanColumn.tsx — same interfaces, no behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/features/pipeline/KanbanCard.tsx
@src/features/pipeline/KanbanColumn.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish KanbanColumn — replace bg-white dark:bg-card with bg-card</name>
  <files>src/features/pipeline/KanbanColumn.tsx</files>
  <action>
In KanbanColumn.tsx make these targeted changes:

1. Header div class: replace `bg-white dark:bg-card` with `bg-card` (Phase 1 token handles dark mode automatically via CSS variable).

2. Outer column div: the current `bg-muted/10` is fine — leave it. Just ensure `border-border` is used (it already is).

3. Drop zone `max-h` value: change from `max-h-[calc(100vh-280px)]` to `max-h-[calc(100vh-var(--topbar-h,280px))]` — this allows the topbar height to be controlled by the token if Phase 7 defines it, with 280px as fallback.

No prop/interface changes. No behavior changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "KanbanColumn" | head -5</automated>
  </verify>
  <done>KanbanColumn.tsx has bg-card on header div (not bg-white dark:bg-card), max-h uses --topbar-h var, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Polish KanbanCard — tighten token usage and hover state</name>
  <files>src/features/pipeline/KanbanCard.tsx</files>
  <action>
In KanbanCard.tsx make these targeted changes:

1. Card root div classes: current is `p-3 rounded-lg border border-border bg-card hover:shadow-sm transition-shadow cursor-pointer select-none group`. Change hover to `hover:shadow-[0_2px_8px_hsl(var(--accent)/0.15)]` for a token-aware shadow that uses the Phase 1 accent color.

2. Avatar div: no change needed — it uses inline `style={{ background: bgColor }}` which is already dynamic.

3. Priority badge `className`: the PRIORIDADE_STYLE map uses raw Tailwind color classes (bg-gray-100, bg-blue-100, etc.) — these are intentional semantic colors for priority levels, leave them unchanged.

4. Tag spans: `backgroundColor: \`${tag.cor}20\`` inline style — leave unchanged, these are user-defined tag colors.

5. Add `ring-1 ring-inset ring-border/0 hover:ring-border/30` to the card root div to give a subtle ring on hover using the border token.

Final card root div classes:
```
p-3 rounded-lg border border-border bg-card hover:shadow-[0_2px_8px_hsl(var(--accent)/0.15)] hover:ring-1 hover:ring-border/30 transition-all cursor-pointer select-none group
```

No prop/interface changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "KanbanCard" | head -5</automated>
  </verify>
  <done>KanbanCard.tsx root div uses token-aware hover shadow, compiles without errors, KanbanCardProps interface unchanged</done>
</task>

</tasks>

<verification>
Run: npx tsc --noEmit
Expected: 0 errors
Grep check: grep "bg-white" src/features/pipeline/KanbanColumn.tsx
Expected: no output (bg-white removed)
Grep check: grep "bg-card" src/features/pipeline/KanbanColumn.tsx
Expected: at least 1 match on the header div
</verification>

<success_criteria>
- KanbanColumn.tsx: no bg-white class, header uses bg-card, drop zone uses --topbar-h var
- KanbanCard.tsx: hover shadow uses hsl(var(--accent)/...) token
- Both files compile without TypeScript errors
- KanbanCardProps and KanbanColumnProps interfaces unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-liderhub-atendimento/03-03-SUMMARY.md`
</output>
