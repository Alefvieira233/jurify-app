---
phase: 11-ux-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/automations/FluxosManager.tsx
  - src/features/automations/RegrasManager.tsx
  - src/features/contracts/ContratosManager.tsx
  - src/features/automations/FlowEditor.tsx
  - src/features/ai-agents/BaseConhecimento.tsx
  - src/features/ai-agents/EnhancedAIChat.tsx
autonomous: true
requirements: [UX-05, UX-06, UX-07, UX-08, UX-10]

must_haves:
  truths:
    - "FluxosManager and RegrasManager action buttons visible without hover"
    - "ContratosManager uses no inline fontFamily styles"
    - "FlowEditor reads colorMode from system theme"
    - "BaseConhecimento shows functional placeholder, not dead stub"
    - "EnhancedAIChat uses onKeyDown, not onKeyPress"
  artifacts:
    - path: "src/features/automations/FluxosManager.tsx"
      provides: "Always-visible action buttons"
      contains: "opacity"
    - path: "src/features/automations/FlowEditor.tsx"
      provides: "System-aware colorMode"
      contains: "colorMode"
---

<objective>
Fix 5 component-specific UI issues: make action buttons touch-accessible, remove inline fonts, fix FlowEditor color mode, replace BaseConhecimento dead stub, replace deprecated onKeyPress.

Purpose: Fix targeted UX issues that affect specific components.
Output: 6 files with improved accessibility and consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Fix touch accessibility and inline fonts</name>
  <files>
    src/features/automations/FluxosManager.tsx,
    src/features/automations/RegrasManager.tsx,
    src/features/contracts/ContratosManager.tsx
  </files>
  <read_first>
    src/features/automations/FluxosManager.tsx,
    src/features/automations/RegrasManager.tsx,
    src/features/contracts/ContratosManager.tsx
  </read_first>
  <action>
**UX-05 — Make hover-only action buttons always visible:**

In `FluxosManager.tsx` and `RegrasManager.tsx`, action buttons (Edit/Delete) on cards are only visible on hover via CSS like `opacity-0 group-hover:opacity-100` or `hidden group-hover:flex`. Change to always visible:

- Remove `opacity-0 group-hover:opacity-100` — replace with `opacity-70 hover:opacity-100` (subtle but always visible)
- Or remove `hidden group-hover:flex` — replace with `flex` (always shown)
- Keep the hover enhancement for mouse users but ensure buttons are accessible without hover

**UX-06 — Remove inline fontFamily from ContratosManager:**

In `ContratosManager.tsx`, remove all `style={{ fontFamily: '...' }}` inline styles. The fonts referenced (`Cormorant Garamond`, `Space Grotesk`) are not loaded in the app. Replace with Tailwind classes:

- `fontFamily: 'Cormorant Garamond'` → `font-serif` (uses system serif) or just remove
- `fontFamily: 'Space Grotesk'` → remove (Inter is the app font)
- Remove all `style={{ fontFamily: ... }}` props entirely — the design tokens handle typography
  </action>
  <verify>
    <automated>cd E:/Jurify && grep -n "fontFamily" src/features/contracts/ContratosManager.tsx && grep -n "group-hover:opacity-100\|hidden group-hover:flex" src/features/automations/FluxosManager.tsx src/features/automations/RegrasManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fontFamily" src/features/contracts/ContratosManager.tsx` returns zero matches
    - FluxosManager.tsx action buttons do not use `opacity-0` or `hidden` as base state
    - RegrasManager.tsx action buttons do not use `opacity-0` or `hidden` as base state
    - `npm run build` passes
  </acceptance_criteria>
  <done>Action buttons always visible on touch devices; inline font references removed</done>
</task>

<task type="auto">
  <name>Task 2: Fix FlowEditor colorMode, BaseConhecimento stub, and deprecated onKeyPress</name>
  <files>
    src/features/automations/FlowEditor.tsx,
    src/features/ai-agents/BaseConhecimento.tsx,
    src/features/ai-agents/EnhancedAIChat.tsx
  </files>
  <read_first>
    src/features/automations/FlowEditor.tsx,
    src/features/ai-agents/BaseConhecimento.tsx,
    src/features/ai-agents/EnhancedAIChat.tsx
  </read_first>
  <action>
**UX-07 — FlowEditor colorMode from system theme:**

In `FlowEditor.tsx`, find `colorMode="dark"` on the ReactFlow component. Replace with dynamic detection:

```tsx
// Add at top of component:
const isDark = document.documentElement.classList.contains('dark');

// Replace:
<ReactFlow colorMode="dark" ...>
// With:
<ReactFlow colorMode={isDark ? 'dark' : 'light'} ...>
```

Or if the app uses a theme context/hook, use that instead. Check if `useTheme` from shadcn exists.

**UX-08 — BaseConhecimento placeholder:**

In `BaseConhecimento.tsx`, the page is a non-functional stub. Replace with a proper "Coming Soon" placeholder:

```tsx
import { BookOpen, Upload } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';

export default function BaseConhecimento() {
  return (
    <div className="flex-1 overflow-auto p-6">
      <div className="flex items-center gap-2 mb-6">
        <BookOpen className="h-5 w-5" />
        <h1 className="text-xl font-semibold">Base de Conhecimento</h1>
      </div>
      <Card className="max-w-lg mx-auto mt-12">
        <CardHeader className="text-center">
          <Upload className="h-12 w-12 mx-auto text-muted-foreground mb-2" />
          <CardTitle>Em breve</CardTitle>
        </CardHeader>
        <CardContent className="text-center text-muted-foreground">
          <p>A base de conhecimento para documentos RAG esta em desenvolvimento.</p>
          <p className="mt-2 text-sm">Em breve voce podera fazer upload de documentos para enriquecer as respostas da IA.</p>
        </CardContent>
      </Card>
    </div>
  );
}
```

**UX-10 — Replace deprecated onKeyPress:**

In `EnhancedAIChat.tsx`, find `onKeyPress` and replace with `onKeyDown`:
```tsx
// FROM:
onKeyPress={(e) => { if (e.key === 'Enter' && !e.shiftKey) { ... } }}
// TO:
onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); ... } }}
```
Note: Add `e.preventDefault()` in onKeyDown to prevent the Enter from adding a newline.
  </action>
  <verify>
    <automated>cd E:/Jurify && grep -n 'colorMode="dark"' src/features/automations/FlowEditor.tsx && grep -n "onKeyPress" src/features/ai-agents/EnhancedAIChat.tsx && grep -n "Em breve" src/features/ai-agents/BaseConhecimento.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `FlowEditor.tsx` does not contain `colorMode="dark"` as a hardcoded string
    - `FlowEditor.tsx` uses dynamic colorMode based on theme detection
    - `BaseConhecimento.tsx` shows "Em breve" placeholder with proper Card layout
    - `EnhancedAIChat.tsx` uses `onKeyDown` instead of `onKeyPress`
    - `grep "onKeyPress" src/features/ai-agents/EnhancedAIChat.tsx` returns zero matches
    - `npm run build` passes
  </acceptance_criteria>
  <done>FlowEditor respects system theme; BaseConhecimento has functional placeholder; onKeyPress replaced</done>
</task>

</tasks>

<verification>
1. `grep "fontFamily" src/features/contracts/ContratosManager.tsx` returns 0 matches
2. `grep 'colorMode="dark"' src/features/automations/FlowEditor.tsx` returns 0 matches
3. `grep "onKeyPress" src/features/ai-agents/EnhancedAIChat.tsx` returns 0 matches
4. `grep "Em breve" src/features/ai-agents/BaseConhecimento.tsx` returns match
5. `npm run build` passes
6. `npm run test` passes
</verification>

<success_criteria>
- UX-05: FluxosManager and RegrasManager buttons visible without hover
- UX-06: ContratosManager uses no inline fontFamily styles
- UX-07: FlowEditor reads colorMode from system theme
- UX-08: BaseConhecimento shows "Em breve" placeholder
- UX-10: EnhancedAIChat uses onKeyDown
- All tests pass, 0 TS errors
</success_criteria>
