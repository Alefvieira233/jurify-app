---
phase: 07-liderhub-routes-final-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
autonomous: true
requirements: [FR-2, FR-9, FR-10, FR-11, FR-12, NFR-1, NFR-2, NFR-4]
must_haves:
  truths:
    - "Sidebar shows Módulos Jurídicos section with Processos, Prazos, Honorários, Documentos links"
    - "Sidebar shows Relatórios section with Relatórios and Métricas links"
    - "Tarefas leaf navigates to /tarefas (not /agendamentos)"
    - "All new sidebar items respect RBAC (adminOnly/managerOk flags where appropriate)"
    - "Zero TypeScript errors after change"
  artifacts:
    - path: src/components/Sidebar.tsx
      provides: "LíderHub nav with all routes wired"
      contains: "juridico"
  key_links:
    - from: src/components/Sidebar.tsx
      to: src/App.tsx
      via: "id values matching route path segments"
      pattern: "id: 'processos'"
---

<objective>
Add the missing Módulos Jurídicos and Relatórios nav sections to Sidebar.tsx, and fix the Tarefas leaf to route to /tarefas.

Purpose: The sidebar currently shows only 8 nav items. All routes built in Phases 2-4 (processos, prazos, honorarios, documentos, tarefas, relatorios, metricas) are registered in App.tsx but unreachable via the sidebar. This plan closes that gap.

Output: Sidebar.tsx with a complete MAIN_NAV structure matching every live route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Sidebar.tsx
@src/App.tsx

<interfaces>
<!-- Current MAIN_NAV leaf IDs (from Sidebar.tsx) that must be PRESERVED unchanged: -->
<!-- home, dashboard, conexoes, whatsapp, crm, pipeline, agentes, base-conhecimento, agendamentos, configuracoes, suporte -->

<!-- Route paths registered in App.tsx that need sidebar entries: -->
<!-- /tarefas          → TarefasPage (built Phase 4) -->
<!-- /processos        → ProcessosManager (built Phase 4) -->
<!-- /prazos           → PrazosManager (built Phase 4) -->
<!-- /honorarios       → HonorariosManager (admin/manager only, built Phase 4) -->
<!-- /documentos       → DocumentosManager (built Phase 4) -->
<!-- /relatorios       → RelatoriosGerenciais -->
<!-- /metricas         → MetricasOperacionais -->

<!-- RBAC in App.tsx: -->
<!-- /honorarios → requiredRoles: ['admin', 'manager'] -->
<!-- /auditoria  → requiredRoles: ['admin', 'manager'] (not in sidebar currently, keep omitted) -->

<!-- Layout.tsx getActiveSection logic: -->
<!-- path.substring(1).split('/')[0] → so id must match first segment of path -->
<!-- Exception: id='home' maps to '/', id='agendamentos' maps to '/agendamentos' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Jurídico + Relatórios sections and fix Tarefas leaf in Sidebar</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
Read src/components/Sidebar.tsx in full first.

Make these targeted changes:

1. ADD new lucide icons to the import block. Append to existing icon imports:
   FileText, Calendar, Scale as ScaleIcon, BarChart2, Activity, Briefcase

   Note: `Scale` is already imported for the logo — import it as `ScaleIcon` via alias or reuse `Scale`. Use `Briefcase` for Processos, `Calendar` for Prazos, `FileText` for Documentos, `Scale` for Honorários (reuse existing import), `BarChart2` for Relatórios, `Activity` for Métricas.

   Exact addition to import list (merge with existing lucide-react import):
   ```
   Briefcase, Calendar, BarChart2, Activity,
   ```
   (FileText and Scale are likely already present or available; check first.)

2. FIX the `agendamentos` leaf — change its `id` from `'agendamentos'` to `'tarefas'` so clicking it navigates to `/tarefas` via `handleSectionChange`. The label stays `'Tarefas'`. The icon stays `CheckSquare`. Per decision: TarefasPage is at /tarefas; AgendamentosManager at /agendamentos is a separate route not needing a sidebar entry.

3. ADD a new `section` entry `'juridico'` after the `automacoes` section and before the `agendamentos` leaf:
   ```typescript
   {
     kind: 'section',
     id: 'juridico',
     label: 'Jurídico',
     icon: Briefcase,
     children: [
       { id: 'processos',  label: 'Processos',   icon: Scale,    resource: 'leads', action: 'read' },
       { id: 'prazos',     label: 'Prazos',      icon: Calendar, resource: 'leads', action: 'read' },
       { id: 'honorarios', label: 'Honorários',  icon: BarChart2, resource: 'leads', action: 'read', adminOnly: true },
       { id: 'documentos', label: 'Documentos',  icon: FileText, resource: 'leads', action: 'read' },
     ],
   },
   ```
   Use `adminOnly: true` for `honorarios` to mirror the `requiredRoles: ['admin', 'manager']` guard in App.tsx. Note: `managerOk: true` should also be set so managers can see it — add `managerOk: true` alongside `adminOnly: true`.

4. ADD a new `section` entry `'relatorios-group'` after the `juridico` section and before the `tarefas` (formerly agendamentos) leaf:
   ```typescript
   {
     kind: 'section',
     id: 'relatorios-group',
     label: 'Relatórios',
     icon: BarChart2,
     children: [
       { id: 'relatorios', label: 'Relatórios',  icon: BarChart2, resource: 'leads', action: 'read' },
       { id: 'metricas',   label: 'Métricas',    icon: Activity,  resource: 'leads', action: 'read' },
     ],
   },
   ```

5. Ensure NO duplicate icon imports (BarChart2 used in two places is fine as it's one import).

6. Check that `FileText` is available from lucide-react (it is); add to import if not present.

Final MAIN_NAV order should be:
  home → dashboard → conexoes → atendimento(section) → automacoes(section) → juridico(section) → relatorios-group(section) → tarefas(leaf) → configuracoes(leaf) → suporte(leaf)
  </action>
  <verify>
    <automated>cd E:/Jurify && npx tsc --noEmit --project tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0
    - Sidebar.tsx contains `id: 'juridico'` section with children processos/prazos/honorarios/documentos
    - Sidebar.tsx contains `id: 'relatorios-group'` section with children relatorios/metricas
    - `id: 'agendamentos'` no longer exists; `id: 'tarefas'` exists as leaf
    - `honorarios` child has `adminOnly: true` and `managerOk: true`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify lint passes after sidebar changes</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
Run lint to confirm zero new warnings. If any unused import warnings appear (e.g., FileText was already imported elsewhere), remove the duplicate from the Sidebar.tsx import line.

Also verify the unit tests still pass:
  npm run test -- --reporter=verbose --run src/components 2>&1 | tail -30

If no component tests exist for Sidebar, that is acceptable — TypeScript check + lint are the verification gate.
  </action>
  <verify>
    <automated>cd E:/Jurify && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run lint` exits 0 with zero errors and zero warnings in Sidebar.tsx
    - No unused variable warnings for any icon import
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — 0 errors
2. `npm run lint` — 0 errors/warnings
3. Grep checks:
   - `grep "id: 'juridico'" src/components/Sidebar.tsx` — 1 match
   - `grep "id: 'relatorios-group'" src/components/Sidebar.tsx` — 1 match
   - `grep "id: 'tarefas'" src/components/Sidebar.tsx` — 1 match (leaf)
   - `grep "id: 'agendamentos'" src/components/Sidebar.tsx` — 0 matches
   - `grep "adminOnly: true" src/components/Sidebar.tsx` — at least 1 match (honorarios)
</verification>

<success_criteria>
- Sidebar has 6 nav sections/leaves at top level (home, dashboard, conexoes, atendimento, automacoes, juridico, relatorios-group, tarefas, configuracoes, suporte = 10 entries)
- Jurídico section expands to show Processos, Prazos, Honorários (admin/manager), Documentos
- Relatórios section expands to show Relatórios and Métricas
- Tarefas leaf routes to /tarefas
- 0 TypeScript errors, 0 lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-liderhub-routes-final-integration/07-01-SUMMARY.md`
</output>
