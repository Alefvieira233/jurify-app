---
phase: 05-kapso-backend-migration
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - supabase/functions/send-whatsapp-message/index.ts
  - supabase/functions/whatsapp-webhook/index.ts
  - supabase/functions/health-check/index.ts
  - supabase/functions/process-prazos-alerts/index.ts
  - supabase/functions/media-processor/index.ts
  - supabase/functions/_shared/kapso-client.ts
  - supabase/functions/_shared/media-utils.ts
  - src/tests/integration/whatsapp-webhook.test.ts
autonomous: true
requirements: [FR-13, FR-14, FR-15, FR-16, FR-19, FR-20, NFR-1, NFR-2, NFR-6]

must_haves:
  truths:
    - "Zero 'evolution' or 'Evolution' strings anywhere in supabase/functions/"
    - "Zero 'evolution' or 'Evolution' strings anywhere in src/tests/"
    - "All existing tests pass (1009+)"
    - "No TypeScript errors, no lint warnings"
    - "health-check, process-prazos-alerts, media-processor are confirmed Kapso-native"
  artifacts:
    - path: "supabase/functions/health-check/index.ts"
      provides: "Health check using checkKapsoHealth()"
      contains: "checkKapsoHealth"
    - path: "supabase/functions/process-prazos-alerts/index.ts"
      provides: "Deadline alerts using sendTextMessage from kapso-client"
      contains: "sendTextMessage"
    - path: "supabase/functions/media-processor/index.ts"
      provides: "Media processor using downloadKapsoMedia"
      contains: "downloadKapsoMedia"
  key_links:
    - from: "supabase/functions/health-check/index.ts"
      to: "supabase/functions/_shared/kapso-client.ts"
      via: "import { checkKapsoHealth }"
      pattern: "checkKapsoHealth"
    - from: "supabase/functions/process-prazos-alerts/index.ts"
      to: "supabase/functions/_shared/kapso-client.ts"
      via: "import { sendTextMessage }"
      pattern: "sendTextMessage"
---

<objective>
Final verification sweep across all Edge Functions and tests to confirm zero Evolution API remnants, then run the full test suite to validate NFR-1 (1009+ tests passing) and NFR-2 (zero TypeScript/lint errors).

Purpose: This is the gate-check plan. After Plans 01 and 02 do the heavy lifting, this plan verifies the entire migration is complete and nothing was missed.
Output: Verified clean codebase with all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-kapso-backend-migration/05-01-SUMMARY.md
@.planning/phases/05-kapso-backend-migration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep all Edge Functions and shared modules for Evolution remnants</name>
  <files>supabase/functions/send-whatsapp-message/index.ts, supabase/functions/whatsapp-webhook/index.ts, supabase/functions/health-check/index.ts, supabase/functions/process-prazos-alerts/index.ts, supabase/functions/media-processor/index.ts, supabase/functions/_shared/kapso-client.ts, supabase/functions/_shared/media-utils.ts</files>
  <read_first>supabase/functions/send-whatsapp-message/index.ts, supabase/functions/whatsapp-webhook/index.ts</read_first>
  <action>
1. Run a comprehensive grep across ALL Edge Function files:
   ```bash
   grep -ri "evolution" supabase/functions/ --include="*.ts"
   ```

2. If ANY matches are found, fix them:
   - Comments mentioning Evolution -> update to Kapso
   - Variable names with Evolution -> rename to Kapso
   - String literals with 'evolution' -> replace with 'kapso'

3. Also check the test directory:
   ```bash
   grep -ri "evolution" src/tests/ --include="*.ts"
   ```

4. Fix any remaining matches found.

5. Verify the three "already done" Edge Functions are confirmed clean:
   - health-check/index.ts: must import and use `checkKapsoHealth` from kapso-client.ts
   - process-prazos-alerts/index.ts: must import and use `sendTextMessage` from kapso-client.ts
   - media-processor/index.ts: must import and use `downloadKapsoMedia` from media-utils.ts

6. If all grep checks return zero matches, no file modifications needed -- just document the verification.
  </action>
  <verify>
    <automated>cd /e/Jurify && MATCHES=$(grep -ri "evolution" supabase/functions/ --include="*.ts" 2>/dev/null | wc -l) && TEST_MATCHES=$(grep -ri "evolution" src/tests/ --include="*.ts" 2>/dev/null | wc -l) && echo "Functions: $MATCHES matches, Tests: $TEST_MATCHES matches" && [ "$MATCHES" -eq 0 ] && [ "$TEST_MATCHES" -eq 0 ] && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "evolution" supabase/functions/ --include="*.ts"` returns 0 matches
    - `grep -ri "evolution" src/tests/ --include="*.ts"` returns 0 matches
    - health-check imports checkKapsoHealth
    - process-prazos-alerts imports sendTextMessage from kapso-client
    - media-processor imports downloadKapsoMedia from media-utils
  </acceptance_criteria>
  <done>Zero Evolution references across entire supabase/functions/ and src/tests/ directories. All Edge Functions confirmed Kapso-native.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and lint to validate NFR-1 and NFR-2</name>
  <files></files>
  <read_first></read_first>
  <action>
1. Run the full test suite:
   ```bash
   npm run test -- --run
   ```
   Verify 1009+ tests pass, 0 failures.

2. Run TypeScript type check:
   ```bash
   npx tsc --noEmit
   ```
   Verify 0 errors.

3. Run linter:
   ```bash
   npm run lint
   ```
   Verify 0 errors, 0 warnings.

4. Run production build to verify no build-time issues:
   ```bash
   npm run build
   ```
   Verify successful build.

If any test fails, investigate whether the failure is caused by the Evolution->Kapso rename (fix it) or is a pre-existing flaky test (document it).
  </action>
  <verify>
    <automated>cd /e/Jurify && npm run test -- --run 2>&1 | tail -3 && npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` shows 1009+ tests passed, 0 failed
    - `npm run lint` shows 0 errors
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Full test suite passes. Zero TypeScript errors. Zero lint warnings. Production build succeeds. Phase 5 migration is complete.</done>
</task>

</tasks>

<verification>
- `grep -ri "evolution" supabase/functions/ src/tests/ --include="*.ts"` returns absolutely zero matches
- `npm run test -- --run` passes 1009+ tests
- `npm run lint` clean
- `npm run build` succeeds
- All 6 Edge Functions (send-whatsapp-message, whatsapp-webhook, health-check, process-prazos-alerts, media-processor, kapso-manager) use Kapso API exclusively
</verification>

<success_criteria>
- Zero Evolution API references in entire codebase (functions + tests)
- 1009+ tests passing (NFR-1)
- Zero TypeScript errors (NFR-2)
- Zero lint warnings (NFR-2)
- Production build succeeds
- DB migration ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/05-kapso-backend-migration/05-03-SUMMARY.md`
</output>
