---
phase: 05-kapso-backend-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/integration/whatsapp-webhook.test.ts
  - supabase/migrations/20260329000001_kapso_cleanup.sql
autonomous: true
requirements: [FR-19, NFR-1]

must_haves:
  truths:
    - "Integration tests reference Kapso instead of Evolution"
    - "Tests still pass with renamed mocks and functions"
    - "DB constraint no longer allows 'evolution' as a connection type for new rows"
  artifacts:
    - path: "src/tests/integration/whatsapp-webhook.test.ts"
      provides: "Updated integration tests with Kapso naming"
      contains: "MOCK_KAPSO_MESSAGE_UPSERT"
    - path: "supabase/migrations/20260329000001_kapso_cleanup.sql"
      provides: "Final DB cleanup removing evolution from constraint"
      contains: "CHECK (tipo IN"
  key_links:
    - from: "src/tests/integration/whatsapp-webhook.test.ts"
      to: "supabase/functions/whatsapp-webhook/index.ts"
      via: "Tests validate the same normalization logic"
      pattern: "normalizeKapsoMessage"
---

<objective>
Update integration tests to use Kapso naming and create a final DB migration to remove the 'evolution' value from the conexoes_whatsapp tipo constraint.

Purpose: Complete the migration by ensuring tests and database schema reflect the new Kapso-only reality. The previous migration (20260325000001) kept 'evolution' in the constraint for backward compatibility -- this plan finalizes the cutover.
Output: Passing tests with Kapso naming, clean DB constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/tests/integration/whatsapp-webhook.test.ts
@supabase/migrations/20260325000001_kapso_migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Evolution references to Kapso in webhook integration tests</name>
  <files>src/tests/integration/whatsapp-webhook.test.ts</files>
  <read_first>src/tests/integration/whatsapp-webhook.test.ts</read_first>
  <action>
In src/tests/integration/whatsapp-webhook.test.ts, perform these renames throughout the entire file:

1. RENAME MOCK CONSTANTS:
   - `MOCK_EVOLUTION_MESSAGE_UPSERT` -> `MOCK_KAPSO_MESSAGE_UPSERT`
   - `MOCK_EVOLUTION_CONNECTION_UPDATE` -> `MOCK_KAPSO_CONNECTION_UPDATE`
   - `MOCK_EVOLUTION_QRCODE` -> `MOCK_KAPSO_QRCODE`
   - `MOCK_EVOLUTION_FROM_ME` -> `MOCK_KAPSO_FROM_ME`

2. RENAME FUNCTIONS (local test copies):
   - `isEvolutionPayload` -> `isKapsoPayload`
   - `normalizeEvolutionMessage` -> `normalizeKapsoMessage`

3. UPDATE PROVIDER VALUES:
   - All occurrences of `provider: 'evolution'` -> `provider: 'kapso'`

4. UPDATE DESCRIBE BLOCKS AND TEST NAMES:
   - "Evolution API payload" -> "Kapso API payload"
   - "Evolution API Normalization" -> "Kapso API Normalization"
   - "Evolution extended text message" -> "Kapso extended text message"
   - "Evolution image message" -> "Kapso image message"
   - "Evolution audio message" -> "Kapso audio message"
   - "extracts Evolution message ID" -> "extracts Kapso message ID"

5. UPDATE HEADER COMMENT (lines 1-11):
   - "Normalização de payloads Evolution API e Meta Official" -> "Normalização de payloads Kapso API e Meta Official"
   - "Envio de resposta via Evolution ou Meta" -> "Envio de resposta via Kapso ou Meta"

6. UPDATE THE NormalizedMessage INTERFACE in the test file:
   - `provider: 'evolution' | 'meta'` -> `provider: 'kapso' | 'meta'`

7. UPDATE `getMessageId` function parameter:
   - `provider: 'evolution' | 'meta'` -> `provider: 'kapso' | 'meta'`
   - The `if (provider === 'evolution')` check -> `if (provider === 'kapso')`

Keep all test logic, assertions, and mock data structures identical. Only rename identifiers and string values.
  </action>
  <verify>
    <automated>cd /e/Jurify && grep -ci "evolution" src/tests/integration/whatsapp-webhook.test.ts | grep "^0$" && npm run test -- --run src/tests/integration/whatsapp-webhook.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "evolution" src/tests/integration/whatsapp-webhook.test.ts` returns 0
    - `grep -c "MOCK_KAPSO_MESSAGE_UPSERT" src/tests/integration/whatsapp-webhook.test.ts` returns >= 1
    - `grep -c "isKapsoPayload" src/tests/integration/whatsapp-webhook.test.ts` returns >= 1
    - `grep -c "normalizeKapsoMessage" src/tests/integration/whatsapp-webhook.test.ts` returns >= 1
    - `grep -c "provider: 'kapso'" src/tests/integration/whatsapp-webhook.test.ts` returns >= 1
    - `npm run test -- --run src/tests/integration/whatsapp-webhook.test.ts` passes all tests
  </acceptance_criteria>
  <done>All test mocks, functions, and assertions use Kapso naming. Tests pass. Zero Evolution references in test file.</done>
</task>

<task type="auto">
  <name>Task 2: Create final DB migration to remove 'evolution' from conexoes_whatsapp constraint</name>
  <files>supabase/migrations/20260329000001_kapso_cleanup.sql</files>
  <read_first>supabase/migrations/20260325000001_kapso_migration.sql</read_first>
  <action>
Create supabase/migrations/20260329000001_kapso_cleanup.sql with this exact content:

```sql
-- Final Kapso migration cleanup
-- Removes 'evolution' from conexoes_whatsapp tipo constraint.
-- All existing 'evolution' rows were already migrated to 'kapso' by 20260325000001_kapso_migration.sql.

-- Drop the old constraint that still allows 'evolution'
ALTER TABLE conexoes_whatsapp
  DROP CONSTRAINT IF EXISTS conexoes_whatsapp_tipo_check;

-- Add clean constraint without 'evolution'
ALTER TABLE conexoes_whatsapp
  ADD CONSTRAINT conexoes_whatsapp_tipo_check
  CHECK (tipo IN ('kapso', 'oficial', 'cloud_api'));

-- Safety: verify no rows have tipo='evolution' (would fail constraint)
-- If any exist, the ALTER above would have failed, which is correct behavior.
```

This is a pure DDL migration. The previous migration (20260325000001) already UPDATE'd all evolution rows to kapso, so this constraint change is safe.
  </action>
  <verify>
    <automated>cd /e/Jurify && test -f supabase/migrations/20260329000001_kapso_cleanup.sql && grep "CHECK (tipo IN" supabase/migrations/20260329000001_kapso_cleanup.sql | grep -v "evolution" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260329000001_kapso_cleanup.sql exists
    - `grep "CHECK (tipo IN" supabase/migrations/20260329000001_kapso_cleanup.sql` shows constraint without 'evolution'
    - `grep "evolution" supabase/migrations/20260329000001_kapso_cleanup.sql` only appears in comments (explaining what was removed), not in SQL statements
  </acceptance_criteria>
  <done>DB migration file exists that drops 'evolution' from the tipo constraint. Ready to apply via `supabase db push`.</done>
</task>

</tasks>

<verification>
- `grep -ri "evolution" src/tests/integration/whatsapp-webhook.test.ts` returns zero matches
- `npm run test -- --run src/tests/integration/whatsapp-webhook.test.ts` passes
- Migration file exists and contains correct constraint without 'evolution'
- `npm run test` (full suite) still passes (NFR-1: 1009+ tests)
</verification>

<success_criteria>
- All webhook integration tests pass with Kapso naming
- Full test suite still passes (1009+ tests)
- DB migration ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/05-kapso-backend-migration/05-02-SUMMARY.md`
</output>
