# Story 1.2: Deploy Edge Functions Ausentes e Configurar Sentry

## Status: Draft
## Sprint: 1
## Estimativa: 4 horas
## Prioridade: P1

## Descricao

Incluir 14 Edge Functions ausentes no pipeline de deploy de producao e configurar monitoramento Sentry no Vercel. As funcoes `encrypt-data` e `decrypt-data` sao bloqueadores criticos para toda a cadeia de criptografia de credenciais (Sprint 2). Sem observabilidade (Sentry), erros em producao sao invisiveis. Corrigir tambem a referencia incorreta `customer-portal` -> `create-portal-session` no deploy list.

## Debitos Incluidos

- **DEB-012:** Edge Functions ausentes no deploy list -- 14 funcoes (3h) -- HIGH
- **DEB-011:** Sentry/Monitoring nao configurado no Vercel (1h) -- HIGH

## Criterios de Aceite

- [ ] `curl /functions/v1/admin-create-user` retorna 200 ou 401 (nao 404)
- [ ] `curl /functions/v1/agentes-ia-api` retorna 200 ou 401
- [ ] `curl /functions/v1/create-portal-session` retorna 200 ou 401
- [ ] `curl /functions/v1/data-retention-cleanup` retorna 200 ou 401
- [ ] `curl /functions/v1/decrypt-data` retorna 200 ou 401
- [ ] `curl /functions/v1/encrypt-data` retorna 200 ou 401
- [ ] `curl /functions/v1/extract-document-text` retorna 200 ou 401
- [ ] `curl /functions/v1/generate-document` retorna 200 ou 401
- [ ] `curl /functions/v1/health` retorna 200
- [ ] `curl /functions/v1/ingest-document-from-file` retorna 200 ou 401
- [ ] `curl /functions/v1/process-followup-queue` retorna 200 ou 401
- [ ] `curl /functions/v1/process-prazos-alerts` retorna 200 ou 401
- [ ] `curl /functions/v1/send-push-notification` retorna 200 ou 401
- [ ] `curl /functions/v1/send-whatsapp-message` retorna 200 ou 401
- [ ] Referencia `customer-portal` corrigida para `create-portal-session` no deploy list
- [ ] `VITE_SENTRY_DSN` configurado no Vercel
- [ ] `SENTRY_AUTH_TOKEN` configurado no Vercel
- [ ] Sentry recebendo erros de producao com source maps
- [ ] Testes existentes continuam passando
- [ ] Pipeline de deploy nao quebra com as novas funcoes

## Dependencias

- Depende de: nenhuma -- pode ser iniciada imediatamente
- Bloqueia: Story 2.1 (DEB-004 e DEB-044 dependem de encrypt-data/decrypt-data em producao), Story 4.2 (DEB-036 depende de DEB-011)

## Tarefas

### DEB-012: Edge Functions Deploy (3h)
- [ ] Abrir `.github/workflows/deploy-production.yml`
- [ ] Localizar a lista de Edge Functions no deploy step
- [ ] Remover referencia incorreta `customer-portal`
- [ ] Adicionar as 14 funcoes ausentes a lista de deploy:
  - `admin-create-user`
  - `agentes-ia-api`
  - `create-portal-session`
  - `data-retention-cleanup`
  - `decrypt-data`
  - `encrypt-data`
  - `extract-document-text`
  - `generate-document`
  - `health`
  - `ingest-document-from-file`
  - `process-followup-queue`
  - `process-prazos-alerts`
  - `send-push-notification`
  - `send-whatsapp-message`
- [ ] Verificar que cada funcao tem diretorio correspondente em `supabase/functions/`
- [ ] Verificar que cada funcao compila sem erros (`deno check`)
- [ ] Verificar que cada funcao importa `_shared/cors.ts` corretamente
- [ ] Fazer deploy manual de teste para validar (`supabase functions deploy {fn}`)
- [ ] Testar cada funcao com curl apos deploy

### DEB-011: Sentry Config (1h)
- [ ] Obter `VITE_SENTRY_DSN` do projeto Sentry
- [ ] Obter `SENTRY_AUTH_TOKEN` do Sentry
- [ ] Configurar ambas as variaveis no Vercel Dashboard (Settings > Environment Variables)
- [ ] Verificar que `vite.config.ts` tem plugin Sentry configurado para source maps
- [ ] Fazer deploy e verificar no Sentry Dashboard que erros chegam
- [ ] Verificar que source maps estao resolvendo corretamente (stack traces legíveis)

### Finalizacao
- [ ] Code review (mudancas em deploy-production.yml)
- [ ] QA validation (curl em todas as 14 funcoes)

## Notas Tecnicas

### Lista Completa de Edge Functions Apos Correcao

O `deploy-production.yml` deve conter TODAS as funcoes que existem em `supabase/functions/` (excluindo `_shared/`). Verificar cruzando:
```bash
ls -d supabase/functions/*/  | grep -v _shared | sed 's|supabase/functions/||;s|/||'
```

### Prioridade de Deploy
1. `encrypt-data` e `decrypt-data` -- bloqueadores para Sprint 2
2. `data-retention-cleanup` -- operacional para compliance
3. `process-followup-queue` e `process-prazos-alerts` -- operacionais para CRM
4. `send-push-notification` e `send-whatsapp-message` -- operacionais para comunicacao
5. Restantes -- funcionalidade geral

### Risco
- **BAIXO**: Adicionar funcoes ao deploy list e aditivo. Se alguma funcao causar erro, pode ser removida da lista sem afetar as demais.
- Verificar se funcoes tem secrets/env vars necessarias configuradas no Supabase Dashboard.

### Arquivos a Modificar
- `.github/workflows/deploy-production.yml` -- deploy list
- Vercel Dashboard -- environment variables (DEB-011)

## Definition of Done

- [ ] Codigo implementado e revisado
- [ ] Testes passando (unit + integration)
- [ ] Lint e TypeScript sem erros
- [ ] Todas as 14 funcoes respondendo em producao (nao 404)
- [ ] Sentry recebendo erros com source maps
- [ ] Deploy verificado em staging
- [ ] Debitos DEB-011 e DEB-012 marcados como resolvidos no assessment

## File List

*A ser preenchido durante implementacao*
